---
phase: 24
plan: 01
subsystem: docs
tags: [docs, latex, overnight-integration, pdf-rebuild]
dependency_graph:
  requires:
    - .planning/phases/21-numerical-recovery/SUMMARY.md
    - results/raman/phase22/SUMMARY.md
    - .planning/phases/23-matched-baseline/SUMMARY.md
  provides:
    - docs-aligned-with-overnight-sibling-sessions
    - rebuilt-pdfs-current-with-tex
  affects:
    - docs/companion_explainer.tex
    - docs/verification_document.tex
    - docs/*.pdf
tech_stack:
  added: []
  patterns: [character-exact tex edits, two-pass pdflatex rebuild, figure staging into docs/figures/]
---

# Phase 24 Plan 01 — Overnight Integration into Canonical Docs

Single executable plan. All edits land in one commit to mirror Phase 20's
pattern. The prompt forbids new PDFs / new reports — only the three
canonical docs and the phase SUMMARY are produced.

## Tasks

### T1 — `docs/verification_document.tex` §sec:april-hessian: replace `flagged` dB caveat

Replace the existing Phase-18-validator flagged block (lines ~1401–1423,
the "Caveat on the dB anchoring of the Hessian-study optima" block)
with a **Phase 21 numerical-recovery** flagged block. Content:

- Original JLD2 metadata: `-60.54 dB` (SMF-28), `-74.45 dB` (HNLF).
- Phase 18 audit recompute (now superseded): `-48.2 dB`, `-44.0 dB`.
- **Phase 21 honest recovery (authoritative)**: `-66.61 dB` (SMF-28,
  `Nt=16384, T=54 ps`, BC `8.10e-04`) and `-86.68 dB` (HNLF,
  `Nt=65536, T=320 ps`, BC `2.24e-04`, 50-iter unconverged).
- Note that HNLF required a much larger window than the Phase 18
  validator used, so the Phase 18 HNLF number was itself
  under-windowed; the correct HNLF anchor is *deeper* than originally
  reported, not shallower.
- Eigenstructure verdict stands (2.6% / 0.41%, indefinite, 100%
  same-sign wings over 20 eigenpairs).
- Cite `.planning/phases/21-numerical-recovery/SUMMARY.md` and the
  two reanchor markdown files.

Grep acceptance:
- `grep -c 'Phase 18'` remains in the block (as superseded value).
- `grep -c '66.61'` and `grep -c '86.68'` each $\geq 1$.
- `grep -c 'Phase 21'` $\geq 1$ somewhere in the §sec:april-hessian
  region.

### T2 — `docs/verification_document.tex`: new subsection §sec:april-sharpness

Insert immediately after §sec:april-hessian (before §sec:april-det). New
subsection title: "Flattening the basin: sharpness-aware regularisation
(Phase 22)". Content must include:

- One-sentence framing: "Phase 13 showed the L-BFGS optima are
  indefinite saddles; Phase 22 asks whether any regulariser produces a
  positive-definite minimum. Answer: no."
- One `keyresult` box with the headline: all 26 regularised optima
  across `\{plain, MC, SAM, trH\}\times\{canonical, pareto57\}`
  remained Hessian-indefinite.
- A compact table of the best 3 trade points per operating point
  (plain baseline, best trH, cheapest MC).
- Figure: `phase22_pareto.png` with caption citing the regularisation
  flavors and referencing `results/raman/phase22/SUMMARY.md`.
- One `advisory` box noting SAM fell out of contention and
  recommending `trH` as the tolerance-tuning knob.

Grep acceptance:
- `grep -c 'sec:april-sharpness'` $\geq 1$.
- `grep -c 'phase22_pareto'` $\geq 1`.
- `grep -c '26 regular'` $\geq 1`.

### T3 — `docs/verification_document.tex`: update v4 100m advisory and §sec:april-wrong W2

- **v4 advisory at lines ~731–767** ("Long-fiber extension at L=100m"):
  - Retract the "50x the length" transferability framing.
  - Replace the -51.50 dB warm-start eval number with a live/historical
    pair: live rerun `-45.52 dB` (Phase 23), historical Phase 16
    `-51.50 dB`, flag the ~6 dB discrepancy as unexplained.
  - Report the Phase 23 matched-quadratic result: `+4 ps^2 \to -45.06 dB`
    (`+0.46 dB` gap to live warm-start), `+1 ps^2 \to -44.35 dB`
    (`+1.17 dB` gap). Conclude: the suppression mechanism at 100 m is
    generic dispersive pre-chirp, not preserved non-quadratic structure.
  - Keep the non-polynomial $R^2 < 0.04$ finding — that stands.
  - Cite `.planning/phases/23-matched-baseline/SUMMARY.md`.
- **§sec:april-wrong W2**: update the convergence caveat with Phase 21's
  independent confirmation (honest rerun gives the same `-54.77 dB` with
  BC `8.47e-06`, so the energy-conservation story survives) and note
  the schema fix (`phi_opt` lives in
  `100m_opt_full_result.jld2`, not the scalar summary).

Grep acceptance:
- `grep -c 'Phase 23'` $\geq 1$ in the 100m region.
- `grep -c '45\.52'` $\geq 1`.
- `grep -c 'matched quadratic\|matched-quadratic'` $\geq 1`.

### T4 — `docs/companion_explainer.tex`: front-load overnight surprises

Insert a `tcolorbox` immediately after the abstract (between the
abstract `end{abstract}` and `\tableofcontents`). Title: "Overnight
update — 2026-04-19/20". Body ~150 words, three bullet surprises:

1. The `-76.86 dB` SMF-28 single-mode optimum is stable (Phase 21
   rerun, Phase 22 replot).
2. The optima are indefinite saddles even after we tried to regularise
   them flat (Phase 22 Pareto, all 26 points indefinite). Best
   tolerance buy is `trH` regularisation: `+0.058 rad` of `\sigma_{3\text{dB}}`
   at `10 dB` of depth cost.
3. The 100 m "50x transferability" story is generic dispersive
   pre-chirp, not structural phase adaptation (Phase 23: a pure
   `+4 ps^2` quadratic matches the live warm-start to within `0.46 dB`).

Close the box by pointing to §16 for the full story. This is the
"first 3 pages" requirement.

Grep acceptance:
- `grep -c 'Overnight update'` $\geq 1`.
- `grep -c '0\.46.*dB\|0\.058.*rad'` $\geq 1`.

### T5 — `docs/companion_explainer.tex` §16 updates

- §16.2 "The optimum is on a razor's edge": append a new paragraph
  "We tried to widen the well." summarising Phase 22 Pareto in
  undergrad language; reference `phase22_pareto.png` inline via
  `\includegraphics`.
- §16.3 "Simple phase is a good initialiser, not a universal minimum":
  append a one-sentence caveat that the `50x` claim does not cover
  the 100m extension (see §16.6 for the Phase 23 update).
- New §16.6 "The 100 m transfer is pre-chirp, not structural":
  2--3 paragraphs explaining Phase 23 in undergrad language,
  including the warm-vs-quadratic overlay figure. Explicitly retract
  the earlier framing.
- §16.5 "What did not survive": add bullet for the Sweep-1 `L=2m,
  P=0.2W` knee (withdrawn, Phase 21 edge-fraction) and bullet for the
  `50x length` framing (withdrawn, Phase 23 matched quadratic).

Grep acceptance:
- `grep -c 'Phase 22'` $\geq 2$ across companion (tcolorbox + §16.2).
- `grep -c 'Phase 23'` $\geq 2$ across companion.
- `grep -c 'matched quadratic\|matched-quadratic\|pre-chirp'` $\geq 2$.

### T6 — Rebuild PDFs

From `docs/`, two passes of `pdflatex -interaction=nonstopmode` on
each of `companion_explainer`, `physics_verification`,
`verification_document`. `physics_verification.tex` is unchanged in
this plan; still rebuild for consistency since the overall doc set
ships together.

Acceptance:
- All three `.pdf` newer than their `.tex`.
- No new `Warning: Reference ... undefined` lines beyond the pre-existing
  missing-image placeholders.
- No non-image `^!` lines in the `.log` files.

### T7 — Write `24-01-SUMMARY.md`

Mirror Phase 20's SUMMARY structure: task table, acceptance
verification, deviations, commit.

### T8 — Commit

Single commit: `docs(24): fold Phase 21/22/23 overnight results into canonical .tex + rebuild PDFs`.

Include:
- `docs/companion_explainer.tex`, `docs/verification_document.tex`
- `docs/companion_explainer.pdf`, `docs/verification_document.pdf`,
  `docs/physics_verification.pdf`
- `docs/figures/phase21_recovered_smf28_phase_profile.png`
- `docs/figures/phase21_100m_phase_profile.png`
- `docs/figures/phase22_pareto.png`
- `docs/figures/phase23_warm_vs_gdd_p1_overlay.png`
- `.planning/phases/24-docs-overnight-integration/**`

## Deviation anticipation

- **Strict workflow guard**: per Phase 20 SUMMARY the PreToolUse hook
  hard-denied Edit inside `/gsd-execute-phase`. The project CLAUDE.md
  explicitly permits flipping `hooks.workflow_guard_strict` to `false`
  for the duration of the task and flipping back. Do it at T1 start,
  flip back after T6; do not commit the flip.

## Out of scope

- No re-runs of any optimisation. No new figures beyond staged overnight
  PNGs (no new PyPlot scripts, no new TikZ).
- `physics_verification.tex` content unchanged — rebuild only.
- No refactor of the section ordering; preserve `\label{sec:april-*}` anchors.
