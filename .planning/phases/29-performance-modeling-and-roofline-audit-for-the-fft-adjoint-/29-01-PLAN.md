# Phase 29 — Plan 01

**Title:** Kernel inventory, benchmark protocol, and roofline decision memo  
**Status:** Complete

## Tasks

1. Enumerate the kernels and call paths that dominate wall-clock time.
2. Define a stable benchmark matrix across grid sizes and thread counts.
3. Specify the modeled outputs: roofline view, serial fractions, tuning verdict.

## Success Criteria

- Kernel list is concrete and tied to files/functions.
- Benchmark protocol is reproducible across host and burst VM.
- The future execution pass can answer "tune more" versus "change algorithm."
