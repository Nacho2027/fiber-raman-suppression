---
mode: quick
quick_id: 260331-gh0
description: Fix sweep methodology — time window formula, max_iter, convergence reporting
date: 2026-03-31
---

# Plan: Fix Sweep Methodology

## Task 1: Fix SPM broadening formula in recommended_time_window()

**Files:** `scripts/common.jl`
**Action:** Replace `Δω_SPM = gamma * P_peak * L_fiber` (gives φ_NL in radians, not frequency) with correct physics: `δω_SPM = 0.86 * φ_NL / T0` where T0 = pulse_fwhm / 1.763 for sech². Add pulse_fwhm kwarg (default 185e-15 s, backward-compatible).
**Verify:** SMF-28 L=5m P=0.20W should now produce ~202 ps window (was 29 ps). L=0.5m P=0.05W should still produce 5 ps (SPM negligible at low φ_NL).
**Done:** Formula updated, docstring corrected.

## Task 2: Increase max_iter from 30 to 60

**Files:** `scripts/run_sweep.jl`
**Action:** Change max_iter=30 to max_iter=60 in both `run_fiber_sweep()` call and `run_multistart()` default.
**Verify:** Both locations updated.
**Done:** max_iter=60 in sweep loop and multistart.

## Task 3: Add suppression-quality reporting

**Files:** `scripts/run_sweep.jl`
**Action:** Add J_after threshold classification (excellent/good/acceptable/poor) to per-point logging and sweep summary. Summary now shows suppression success rate, not just convergence count.
**Verify:** Summary output shows suppression counts alongside convergence.
**Done:** Per-point quality labels and summary with suppression breakdown.
