# Context

The user is now mostly using Codex rather than Claude Code.

That means `AGENTS.md` should not stay as a minimal pointer file. It should
carry the must-follow operational rules directly so Codex can act correctly
without depending on a long second document for basic workflow behavior.
