# Plan

1. Expand `AGENTS.md` from a short note into a Codex-first operational rules file.
2. Keep it shorter than `CLAUDE.md`, but include all non-negotiable workflow,
   compute, output, and results rules.
3. Fix any active `CLAUDE.md` wording that still contradicts the current
   Syncthing + explicit-burst-staging model.
