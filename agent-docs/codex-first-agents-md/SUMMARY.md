# Summary

`AGENTS.md` is now the short canonical agent contract instead of a lightweight
pointer to `CLAUDE.md`.

It now includes the critical rules Codex needs directly:

- docs split
- research and test expectations
- direct-to-`main` git behavior
- Syncthing Mac/host workflow
- burst compute staging rules
- standard image mandate
- results-handling guidance

`CLAUDE.md` remains the longer human/reference document.
