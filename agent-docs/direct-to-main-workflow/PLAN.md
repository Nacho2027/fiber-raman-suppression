# Plan

1. Update `CLAUDE.md` so the multi-machine and parallel-session sections describe direct-to-`main` coordination instead of branch-per-session integration.
2. Update `AGENTS.md` with the same short rule in Codex-facing language.
3. Verify the live docs no longer tell agents to create `sessions/*` branches as the normal workflow.
