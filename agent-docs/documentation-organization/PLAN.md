# Documentation Organization Plan

1. Map existing human docs, agent docs, and current context files.
2. Research graph-style Markdown, Diataxis-style documentation organization,
   and `llms.txt` style agent-readable manifests.
3. Add the missing root map for `agent-docs/`.
4. Add a compact `llms.txt` source map.
5. Update existing indexes so the new docs are discoverable.
6. Validate by reading changed files, checking for Obsidian-only wikilinks, and
   reviewing `git status --short`.
