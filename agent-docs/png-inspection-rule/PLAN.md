# Plan

1. Add a completion rule to `AGENTS.md` requiring visual inspection of generated
   PNG outputs.
2. Add the fuller version to `CLAUDE.md`, including how to handle large batches.
3. Keep the rule strict for single runs and sampled-but-explicit for large
   batches.
