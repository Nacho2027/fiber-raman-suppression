# Plan

1. Install and start Syncthing on the Mac and on `claude-code-host`.
2. Pair the two devices and share the repo working tree with ignore rules for
   `.git` and `.DS_Store`.
3. Update the active workflow docs so they describe Syncthing for Mac/host and
   explicit `rsync` staging for burst.
