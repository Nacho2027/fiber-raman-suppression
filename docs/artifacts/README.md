# Artifacts

Selected figures and report artifacts live here. Do not mirror `results/`.

Add an artifact here only when it is intentionally curated for a report,
presentation, fixture, or long-lived reference.

## Curated Sets

- `mmf-highres-2026-04-28/` — accepted high-resolution GRIN-50 MMF validation
  evidence (`Nt=8192`, `TW=96 ps`, `L=2 m`, `P=0.20 W`). Includes the summary
  table, standard image set, total/per-mode spectra, phase plot, and convergence
  trace used by the MMF readiness report.
