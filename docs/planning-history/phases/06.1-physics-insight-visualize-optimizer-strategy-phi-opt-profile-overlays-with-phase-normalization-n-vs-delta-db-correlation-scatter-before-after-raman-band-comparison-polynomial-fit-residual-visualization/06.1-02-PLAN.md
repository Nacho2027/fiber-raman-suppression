---
phase: 06.1-physics-insight
plan: "02"
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - scripts/physics_insight.jl
  - results/images/insight_05_raman_band_before_after.png
  - results/images/insight_06_group_delay_overlay.png
  - results/images/insight_07_residual_overlay.png
  - results/images/insight_08_phase_raman_zoom.png
autonomous: false

must_haves:
  truths:
    - "Before/after Raman comparison shows input spectrum shape + J values for each run"
    - "Group delay (d_phi/d_omega) reveals temporal reshaping strategy per run"
    - "Polynomial residual overlay shows the 99% unexplained phase structure"
    - "Raman-band zoom reveals whether optimizer targets the 13.2 THz offset specifically"
    - "All 8 figures are visually checked by the user for physics correctness"
  artifacts:
    - path: "results/images/insight_05_raman_band_before_after.png"
      provides: "Input spectrum + J before/after annotations per run"
    - path: "results/images/insight_06_group_delay_overlay.png"
      provides: "Group delay overlay for all 5 runs"
    - path: "results/images/insight_07_residual_overlay.png"
      provides: "Polynomial fit residual — the 99% unexplained structure"
    - path: "results/images/insight_08_phase_raman_zoom.png"
      provides: "Zoom into phi_opt near Raman band offset"
  key_links:
    - from: "scripts/physics_insight.jl (Fig 6)"
      to: "scripts/visualization.jl::_central_diff"
      via: "Group delay = d_phi/d_omega computed with _central_diff"
      pattern: "_central_diff"
    - from: "scripts/physics_insight.jl (Fig 7)"
      to: "scripts/visualization.jl::decompose_phase_polynomial"
      via: "Residual = phi_norm - polynomial fit"
      pattern: "decompose_phase_polynomial"
---

<objective>
Add Figures 5-8 to physics_insight.jl (before/after Raman band, group delay, polynomial residual, Raman zoom) and run a human visual checkpoint on all 8 figures.

Purpose: Figures 5-8 are the derived/analytical views that transform the raw phase data into physically interpretable quantities. The residual overlay (Fig 7) is the most important figure — it visualizes the 99% of phase structure that GDD+TOD cannot explain. The group delay (Fig 6) shows temporal reshaping. The Raman zoom (Fig 8) tests whether the optimizer specifically targets the Raman frequency.

Output: 4 additional PNG files in `results/images/`, visual approval of all 8 figures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-physics-insight-visualize-optimizer-strategy-phi-opt-profile-overlays-with-phase-normalization-n-vs-delta-db-correlation-scatter-before-after-raman-band-comparison-polynomial-fit-residual-visualization/06.1-CONTEXT.md
@.planning/phases/06.1-physics-insight-visualize-optimizer-strategy-phi-opt-profile-overlays-with-phase-normalization-n-vs-delta-db-correlation-scatter-before-after-raman-band-comparison-polynomial-fit-residual-visualization/06.1-RESEARCH.md
@.planning/phases/06.1-physics-insight-visualize-optimizer-strategy-phi-opt-profile-overlays-with-phase-normalization-n-vs-delta-db-correlation-scatter-before-after-raman-band-comparison-polynomial-fit-residual-visualization/06.1-01-SUMMARY.md
@scripts/physics_insight.jl
@scripts/visualization.jl

<interfaces>
<!-- From Plan 01 output (physics_insight.jl): -->
```julia
# Section 1 helpers (created in Plan 01):
function normalize_phase(phi_raw, uomega0, sim_Dt_ps, Nt)
  # Returns (phi_norm, df_THz, signal_mask)
  # phi_norm: fftshifted, detrended, noise-floor zeroed
  # df_THz: fftshifted freq offset grid in THz
  # signal_mask: Bool vector, true where spectrum > -40 dB

function run_label(run)
  # Returns "SMF-28 L=1m P=0.05W N=1.3" or "HNLF L=1m P=0.05W N=8.1 (nc)"

function build_lambda_axis_nm(df_THz, lambda0_nm)
  # Returns (lambda_nm, pos_mask)

# Per-run stored data (from Section 2 loading):
# run["phi_norm"]     — normalized phase (fftshifted, 1D)
# run["df_THz"]       — freq offset grid in THz (fftshifted, 1D)
# run["signal_mask"]  — Bool vector (fftshifted)
# run["decomp"]       — NamedTuple (gdd_fs2, tod_fs3, residual_fraction)
```

From scripts/visualization.jl:
```julia
function _central_diff(y, dx)  # line 176 — first derivative
function add_caption!(fig, caption; fontsize=9, y=0.01)  # line 254
const COLORS_5_RUNS = [...]   # 5-color Okabe-Ito palette
const COLOR_RAMAN = "#CC79A7"
const C_NM_THZ = 299792.458
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Figures 5-8 to physics_insight.jl and execute</name>
  <files>scripts/physics_insight.jl, results/images/insight_05_raman_band_before_after.png, results/images/insight_06_group_delay_overlay.png, results/images/insight_07_residual_overlay.png, results/images/insight_08_phase_raman_zoom.png</files>
  <read_first>
    - scripts/physics_insight.jl (full — understand current structure and where to append)
    - scripts/visualization.jl (lines 176-185 for _central_diff, 254-258 for add_caption!)
    - .planning/phases/06.1-physics-insight-visualize-optimizer-strategy-phi-opt-profile-overlays-with-phase-normalization-n-vs-delta-db-correlation-scatter-before-after-raman-band-comparison-polynomial-fit-residual-visualization/06.1-RESEARCH.md (lines 226-444 for figure-by-figure guide: Figs 5-8)
  </read_first>
  <action>
Append Sections 7-10 to the PROGRAM_FILE guard block in `scripts/physics_insight.jl`, before the final summary log:

**Section 7 — Figure 5: Before/After Raman Band Comparison per D-04 item 5:**
Use Option A (no re-propagation). The JLD2 does NOT store the optimized output field, and re-propagation would add ~2.5 min compute time.

- figsize=(14, 12). Layout: 5 rows x 1 col (one panel per run) OR a 3+2 grid.
- For each run:
  - Compute input power spectrum: `spec_dB = 10 .* log10.(abs2.(fftshift(vec(run["uomega0"]))) ./ P_peak .+ 1e-30)` where P_peak = maximum over ALL runs (for consistent dB normalization) or per-run (for detail).
  - Plot spec_dB vs df_THz. Use COLOR_INPUT.
  - Shade the Raman band region with `axvspan(-30.0, -13.2, alpha=0.08, color=COLOR_RAMAN)`.
  - Annotate with `J_before_dB`, `J_after_dB`, and `delta_J_dB` as text in the upper-right of each panel.
  - Panel title: run_label(run).
  - x-label: "Frequency offset from carrier (THz)". y-label: "Power spectral density (dB)".
  - y-limits: [-60, 5] (or auto based on data with some margin).
- Caption: "Input spectra with Raman band annotated. J values from optimization (no re-propagation)."
- Save to `results/images/insight_05_raman_band_before_after.png`.

**Section 8 — Figure 6: Group Delay Overlay per D-04 item 6:**
- figsize=(12, 6). Single axes.
- For each run:
  - Compute group delay: `dw = 2pi * (df_THz[2] - df_THz[1])` (rad/THz spacing). Then `gd_ps = _central_diff(phi_norm, dw)` — units are rad / (rad/THz) = ps.
  - Apply signal mask: set `gd_ps[.!signal_mask] .= NaN` for clean display.
  - Plot `df_THz` vs `gd_ps` using COLORS_5_RUNS[i], label=run_label(run).
- Shade Raman band: `axvspan(-30.0, -13.2, alpha=0.08, color=COLOR_RAMAN)`.
- x-label: "Frequency offset from carrier (THz)". y-label: "Group delay (ps)".
- Title: "Group delay d_phi/d_omega — temporal reshaping by optimizer".
- Legend.
- Caption: "Group delay shows how the optimizer redistributes pulse arrival time vs frequency."
- Save to `results/images/insight_06_group_delay_overlay.png`.

**Section 9 — Figure 7: Polynomial Fit Residual Overlay per D-04 item 7:**
This is THE most important figure of the phase (per CONTEXT.md specifics and RESEARCH.md).

- figsize=(12, 6). Single axes.
- For each run:
  - Reconstruct the polynomial fit on the FULL fftshifted frequency grid:
    - `omega_rad_s = 2pi .* run["df_THz"] .* 1e12` (THz to rad/s for polynomial evaluation).
    - `gdd_s2 = run["decomp"].gdd_fs2 * 1e-30` (fs^2 to s^2).
    - `tod_s3 = run["decomp"].tod_fs3 * 1e-45` (fs^3 to s^3).
    - `phi_poly = gdd_s2 .* omega_rad_s.^2 ./ 2.0 .+ tod_s3 .* omega_rad_s.^3 ./ 6.0`
  - Compute residual: `residual = phi_norm .- phi_poly`. Apply signal mask: set `residual[.!signal_mask] .= NaN`.
  - Plot `df_THz` vs `residual` using COLORS_5_RUNS[i], label=run_label(run) + @sprintf(" (res %.1f%%)", run["decomp"].residual_fraction * 100).
- Shade Raman band.
- x-label: "Frequency offset from carrier (THz)". y-label: "Phase residual (rad)".
- Title: "Polynomial fit residual — the unexplained phase structure".
- Caption: "Residual after removing GDD+TOD fit. 98.9-99.9% of phase variance lives here."
- Save to `results/images/insight_07_residual_overlay.png`.

**Section 10 — Figure 8: Phase Structure at Raman Offset (Zoom) per D-04 item 8:**
- figsize=(12, 6). Single axes.
- For each run:
  - Extract the Raman-band frequency window: `zoom_mask = (-20.0 .< df_THz .< -8.0) .& signal_mask`.
  - Plot `df_THz[zoom_mask]` vs `phi_norm[zoom_mask]` using COLORS_5_RUNS[i].
- `ax.axvline(-13.2, color=COLOR_RAMAN, lw=1.5, ls="--", label="13.2 THz Raman peak")`.
- x-label: "Frequency offset from carrier (THz)". y-label: "Normalized phase (rad)".
- Title: "Phase structure near Raman band offset".
- xlim=(-20, -8).
- Caption: "Zoom into [-20, -8] THz showing phase behavior at the 13.2 THz Raman shift."
- Save to `results/images/insight_08_phase_raman_zoom.png`.

**Update summary log** to list all 8 generated PNGs.

After writing, execute the full script:
```bash
cd /Users/ignaciojlizama/RiveraLab/smf-gain-noise
julia --project scripts/physics_insight.jl
```

Expected runtime: ~30-60 seconds. Verify all 8 PNGs exist and are > 10 KB.
  </action>
  <verify>
    <automated>ls -la /Users/ignaciojlizama/RiveraLab/smf-gain-noise/results/images/insight_0{1,2,3,4,5,6,7,8}_*.png 2>/dev/null | wc -l | grep -q "8" && echo "ALL 8 FIGURES EXIST" || echo "MISSING FIGURES"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/physics_insight.jl contains `insight_05_raman_band_before_after.png`
    - scripts/physics_insight.jl contains `insight_06_group_delay_overlay.png`
    - scripts/physics_insight.jl contains `insight_07_residual_overlay.png`
    - scripts/physics_insight.jl contains `insight_08_phase_raman_zoom.png`
    - scripts/physics_insight.jl contains `_central_diff` (group delay computation)
    - scripts/physics_insight.jl contains `gdd_s2` or `gdd_fs2.*1e-30` (unit conversion for polynomial reconstruction)
    - scripts/physics_insight.jl contains `J_before_dB` or `J_after_dB` (annotations on Fig 5)
    - scripts/physics_insight.jl contains `-20` and `-8` (Raman zoom xlim range)
    - results/images/insight_05_raman_band_before_after.png exists and is > 10 KB
    - results/images/insight_06_group_delay_overlay.png exists and is > 10 KB
    - results/images/insight_07_residual_overlay.png exists and is > 10 KB
    - results/images/insight_08_phase_raman_zoom.png exists and is > 10 KB
  </acceptance_criteria>
  <done>
    Figures 5-8 added to physics_insight.jl. Script executes successfully. All 8 PNG files exist in results/images/ at > 10 KB each.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 8 physics insight figures</name>
  <files>results/images/insight_01_phi_overlay_freq.png, results/images/insight_02_phi_overlay_lambda.png, results/images/insight_03_phi_detail_panels.png, results/images/insight_04_correlation_scatter.png, results/images/insight_05_raman_band_before_after.png, results/images/insight_06_group_delay_overlay.png, results/images/insight_07_residual_overlay.png, results/images/insight_08_phase_raman_zoom.png</files>
  <action>
Present all 8 PNG figures to the user for visual inspection. The figures are at:
- results/images/insight_01_phi_overlay_freq.png
- results/images/insight_02_phi_overlay_lambda.png
- results/images/insight_03_phi_detail_panels.png
- results/images/insight_04_correlation_scatter.png
- results/images/insight_05_raman_band_before_after.png
- results/images/insight_06_group_delay_overlay.png
- results/images/insight_07_residual_overlay.png
- results/images/insight_08_phase_raman_zoom.png

What was built:
8 exploratory physics figures produced by scripts/physics_insight.jl:
1. insight_01 — Normalized phi_opt overlay (all 5 runs) vs frequency offset (THz), Raman band shaded
2. insight_02 — Same overlay in wavelength (nm) domain
3. insight_03 — 5-panel per-run detail with GDD+TOD polynomial fit overlaid
4. insight_04 — N/L/P vs delta_dB scatter with fiber-type markers
5. insight_05 — Input spectra per run with Raman band annotated and J values
6. insight_06 — Group delay (d_phi/d_omega in ps) showing temporal reshaping
7. insight_07 — THE key figure: polynomial fit residual showing the 99% unexplained phase structure
8. insight_08 — Zoom into [-20, -8] THz near the 13.2 THz Raman offset

Verification checklist for user:
- All 5 runs visible in overlay figures with distinguishable colors
- Axes labeled with correct units (THz, nm, rad, ps, dB)
- Raman band shaded where applicable
- Text readable and not overlapping
- Polynomial fit visibly much smaller than actual phase in detail panels
- Convergence status annotated (filled vs open markers, "(nc)" labels)
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires human visual verification of 8 figures"</automated>
  </verify>
  <done>
    User has visually approved all 8 physics insight figures (or provided specific issues to fix). Figures communicate physics clearly for lab meetings and advisor reviews.
  </done>
</task>

</tasks>

<verification>
After Plan 02 completion:
1. All 8 PNG files exist in `results/images/insight_*.png`
2. Group delay computed via _central_diff (not custom implementation)
3. Polynomial residual uses correct unit conversion (fs^2 -> s^2 via 1e-30)
4. Raman zoom window is [-20, -8] THz
5. User has visually approved all 8 figures
</verification>

<success_criteria>
- All 8 figures from D-04 are produced and visually approved
- Group delay (Fig 6) shows temporal reshaping structure
- Polynomial residual (Fig 7) reveals the shape of the 99% unexplained phase structure
- Raman zoom (Fig 8) tests whether optimizer targets the 13.2 THz offset
- Before/after (Fig 5) shows input spectra with J value annotations
- User confirms figures communicate physics clearly for lab meetings/advisor reviews
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-physics-insight-visualize-optimizer-strategy-phi-opt-profile-overlays-with-phase-normalization-n-vs-delta-db-correlation-scatter-before-after-raman-band-comparison-polynomial-fit-residual-visualization/06.1-02-SUMMARY.md`
</output>
