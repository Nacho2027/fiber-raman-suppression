# Summary: 07.1-01 Grid Resolution Fix

## Status: COMPLETE

## Changes Made

### Task 1: Fix run_sweep.jl

File: `scripts/run_sweep.jl`

1. **Added `SW_NT_FLOOR = 2^13` constant** (line 55) — minimum Nt for optimization quality
2. **Applied Nt floor in `run_fiber_sweep()`** (line 177): `Nt = max(nt_for_window(time_window), SW_NT_FLOOR)`
3. **Applied Nt floor in `run_multistart()`** (line 353): same pattern
4. **Reduced `max_iter` from 100 to 30** in `run_fiber_sweep()` (line 198) and `run_multistart()` default (line 342)
5. **Dropped L=10m from `SW_SMF28_L`** (line 62): now `[0.5, 1.0, 2.0, 5.0]`
6. **Updated comments and docstrings** to reflect 4x4=16 SMF-28 grid (lines 5, 58, 61, 146)

### Task 2: Clean stale results

- Deleted `results/raman/sweeps/smf28/` (15 JLD2 files at Nt=1024-4096 from aborted sweep)
- `results/raman/sweeps/` directory preserved (empty, ready for re-run)

## Acceptance Criteria Verification

- [x] `SW_SMF28_L` no longer contains 10.0
- [x] Nt clamped to minimum 2^13 in `run_fiber_sweep()`
- [x] Nt clamped to minimum 2^13 in `run_multistart()`
- [x] `max_iter=30` in sweep and multistart
- [x] Comments/docstrings updated for 4x4 grid
- [x] Stale results deleted
- [x] No other behavioral changes

## Next Step

User must manually kick off the sweep:
```bash
julia --project scripts/run_sweep.jl
```
Estimated runtime: 3-8 hours (32 sweep points + 10 multi-start at Nt>=8192, max_iter=30).
