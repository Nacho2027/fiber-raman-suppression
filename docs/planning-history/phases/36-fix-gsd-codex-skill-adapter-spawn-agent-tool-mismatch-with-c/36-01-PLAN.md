---
phase: 36
plan: 01
plan_id: 36-01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/reproduce.sh
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/capture_tools.sh
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/bootstrap_test_project.sh
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/reproduction.txt
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/codex_tools.txt
autonomous: true
requirements: []
requirements_addressed:
  - CTX-reproduce-before-fixing
  - CTX-audit-codex-tool-surface
  - CTX-catalogue-affected-skills
  - CTX-throwaway-test-project
user_setup: []

must_haves:
  truths:
    - "The 0-invocation spawn_agent symptom is reproduced on this machine from live sqlite logs"
    - "Codex v0.121.0 tool inventory is captured from the installed native binary"
    - "A throwaway 2-plan test project bootstrap script exists and is runnable"
  artifacts:
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/reproduce.sh
      provides: "Reproduction harness for the spawn_agent=0 symptom"
      contains: "tool_name=\"spawn_agent\""
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/capture_tools.sh
      provides: "Tool-name inventory extraction from Codex binary"
      contains: "strings"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/bootstrap_test_project.sh
      provides: "Throwaway /tmp/gsd-codex-adapter-test/ scaffold creator"
      contains: "01-two-plan-phase"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/reproduction.txt
      provides: "Baseline evidence of the symptom"
      contains: "spawn_agent"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/codex_tools.txt
      provides: "Codex v0.121.0 tool surface"
      contains: "exec_command"
  key_links:
    - from: harness/reproduce.sh
      to: ~/.codex/logs_2.sqlite
      via: sqlite3 COUNT query
      pattern: "tool_name=.spawn_agent"
    - from: harness/capture_tools.sh
      to: Codex native binary (~/.nvm/.../codex-darwin-arm64/.../codex)
      via: strings | grep
      pattern: "spawn_agent|spawn_agents_on_csv"
    - from: harness/bootstrap_test_project.sh
      to: /tmp/gsd-codex-adapter-test/
      via: mkdir + git init + write trivial PLAN files
      pattern: "01-01-PLAN.md"
---

<objective>
Wave 0 reproduction + inventory. Prove the spawn_agent=0 symptom on this machine,
capture the Codex v0.121.0 tool surface from the installed binary, and ship a
bootstrap script that creates the throwaway 2-plan test project RESEARCH §9
describes. No adapter edits in this plan — baseline only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-CONTEXT.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-VALIDATION.md

<interfaces>
<!-- Paths and queries that later plans will consume -->
<!-- All harness scripts live under .planning/phases/36-*/harness/ and -->
<!-- MUST produce evidence under .planning/phases/36-*/evidence/ -->

Reproduction query (from RESEARCH §2, literal):
```
sqlite3 ~/.codex/logs_2.sqlite \
  "SELECT COUNT(*) FROM logs WHERE feedback_log_body LIKE '%tool_name=\"spawn_agent\"%';"
```

Codex binary path (from RESEARCH §2):
```
~/.nvm/versions/node/v22.3.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/codex/codex
```
If that exact path does not resolve (different node version), fall back to:
```
find ~/.nvm ~/.codex /usr/local -type f -name codex 2>/dev/null | xargs file 2>/dev/null | grep -E 'Mach-O|ELF' | head -1
```

Throwaway test project scaffold (from RESEARCH §9):
- /tmp/gsd-codex-adapter-test/ — empty git repo
- /tmp/gsd-codex-adapter-test/.planning/ROADMAP.md — one phase, two plans
- /tmp/gsd-codex-adapter-test/.planning/phases/01-two-plan-phase/
  - 01-CONTEXT.md — trivial
  - 01-01-PLAN.md — "create file A.txt with content hello"
  - 01-02-PLAN.md — "create file B.txt with content world"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write reproduce.sh and capture_tools.sh harness scripts</name>
  <read_first>
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md (§2 Reproduction Report and §3 Tool Inventory)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <files>
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/reproduce.sh,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/capture_tools.sh
  </files>
  <action>
    Create `.planning/phases/36-*/harness/` directory (mkdir -p).

    Write `harness/reproduce.sh` as a bash script with `set -euo pipefail` that:
    1. Prints `codex --version` (captures "codex-cli 0.121.0").
    2. Prints content of `~/.codex/get-shit-done/VERSION` if present, else `echo "VERSION file missing"`.
    3. Counts skills: `ls ~/.codex/skills/*/SKILL.md | wc -l`.
    4. Counts `spawn_agent` references across skills:
       `grep -l 'spawn_agent' ~/.codex/skills/*/SKILL.md | wc -l`.
    5. Runs the sqlite query over every `~/.codex/logs_*.sqlite` file found (use `ls ~/.codex/logs_*.sqlite 2>/dev/null` and iterate):
       `sqlite3 "$db" "SELECT COUNT(*) FROM logs WHERE feedback_log_body LIKE '%tool_name=\"spawn_agent\"%';"` — print each db path and count.
    6. For one db (first one), also dump distinct tool_names ever observed:
       `sqlite3 "$db" "SELECT feedback_log_body FROM logs WHERE feedback_log_body LIKE '%tool_name=%' LIMIT 50;" | grep -oE 'tool_name="[^"]+"' | sort -u`.
    7. All output streams to stdout (caller tees to evidence/reproduction.txt).

    Make the script chmod +x. The script MUST NOT require any argument.

    Write `harness/capture_tools.sh` as a bash script with `set -euo pipefail` that:
    1. Resolves the Codex native binary path. First try
       `~/.nvm/versions/node/v22.3.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/codex/codex`.
       If not present, fall back to
       `find ~/.nvm ~/.codex /usr/local/lib/node_modules -type f -name codex 2>/dev/null | xargs -I {} file {} 2>/dev/null | grep -E 'Mach-O|ELF' | head -1 | cut -d: -f1`.
    2. Prints the resolved path (or "BINARY NOT FOUND" and exits 2).
    3. Runs `strings "$BIN" | grep -E '^(spawn_agent|spawn_agents_on_csv|exec_command|apply_patch|wait|close_agent|send_input|send_message|resume_agent|followup_task|list_agents|update_plan|request_user_input|web_search_request)$' | sort -u` and prints the result.
    4. Also runs `codex --help 2>&1 | head -40` and `codex exec --help 2>&1 | head -40`.
    5. Also runs `codex features list 2>&1 | head -40` so reviewers can confirm `multi_agent = stable / true`.
    6. All output streams to stdout (caller tees to evidence/codex_tools.txt).

    Make it chmod +x.

    **STOP condition baked into reproduce.sh:** If the total `spawn_agent` COUNT across all logs_*.sqlite is > 0, emit to stderr:
    `echo "STOP: spawn_agent count is $COUNT, research assumed 0. Research may be outdated — re-run /gsd-research-phase before patching." >&2` and exit 3.
    This encodes the first STOP condition from the phase guidance.
  </action>
  <acceptance_criteria>
    - `test -x .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/reproduce.sh`
    - `test -x .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/capture_tools.sh`
    - `grep -q 'tool_name=\\\"spawn_agent\\\"' .planning/phases/36-*/harness/reproduce.sh` (literal escaped double-quote pattern matches the sqlite query)
    - `grep -q 'set -euo pipefail' .planning/phases/36-*/harness/reproduce.sh`
    - `grep -q 'set -euo pipefail' .planning/phases/36-*/harness/capture_tools.sh`
    - `grep -q 'strings' .planning/phases/36-*/harness/capture_tools.sh`
    - `grep -q 'codex features list' .planning/phases/36-*/harness/capture_tools.sh`
    - `grep -q 'STOP: spawn_agent count' .planning/phases/36-*/harness/reproduce.sh` (encodes STOP condition 1)
  </acceptance_criteria>
  <verify>
    <automated>bash -n .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/reproduce.sh && bash -n .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/capture_tools.sh</automated>
  </verify>
  <done>Both scripts exist, are executable, pass `bash -n` (syntax check), and contain the literal strings listed above.</done>
</task>

<task type="auto">
  <name>Task 2: Write bootstrap_test_project.sh and run the two capture scripts</name>
  <read_first>
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md (§9 "Before/after test protocol" — exact throwaway project layout)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/reproduce.sh (from Task 1)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/capture_tools.sh (from Task 1)
  </read_first>
  <files>
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/bootstrap_test_project.sh,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/reproduction.txt,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/codex_tools.txt
  </files>
  <action>
    Write `harness/bootstrap_test_project.sh` as a bash script with `set -euo pipefail` that:
    1. Targets TEST_DIR=${TEST_DIR:-/tmp/gsd-codex-adapter-test}.
    2. `rm -rf "$TEST_DIR" && mkdir -p "$TEST_DIR"`.
    3. `cd "$TEST_DIR" && git init -q && git config user.email test@example.com && git config user.name "Test Bootstrap"`.
    4. `mkdir -p .planning/phases/01-two-plan-phase`.
    5. Writes `.planning/ROADMAP.md` with content:
       ```
       # Roadmap
       ## Phase 1: two-plan-phase
       **Goal:** Create two trivial files across two plans to exercise the GSD execute-phase protocol.
       **Plans:** 2 plans
       - [ ] 01-01-PLAN.md — create A.txt
       - [ ] 01-02-PLAN.md — create B.txt
       ```
    6. Writes `.planning/phases/01-two-plan-phase/01-CONTEXT.md` with a single line: `Trivial test phase for adapter validation.`.
    7. Writes `.planning/phases/01-two-plan-phase/01-01-PLAN.md` with frontmatter `phase: 1`, `plan: 01`, `type: execute`, `wave: 1`, `depends_on: []`, `files_modified: [A.txt]`, `autonomous: true`, plus a single task:
       `<task type="auto"><name>Task: create A.txt</name><files>A.txt</files><action>Write the literal string "hello" (no newline) to A.txt at the repo root.</action><verify><automated>test "$(cat A.txt)" = "hello"</automated></verify><done>A.txt exists with content "hello"</done></task>`.
    8. Writes `.planning/phases/01-two-plan-phase/01-02-PLAN.md` analogously for `B.txt` with content `"world"`, wave=1, no deps on 01.
    9. `git add -A && git commit -q -m "chore: seed test project"`.
    10. Prints `echo "Bootstrapped: $TEST_DIR"`.

    Make it chmod +x.

    After the three harness scripts exist, RUN them to produce baseline evidence:
    - `mkdir -p .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/`
    - `bash .planning/phases/36-*/harness/reproduce.sh > .planning/phases/36-*/evidence/reproduction.txt 2>&1`
      (If the STOP condition fires — exit 3 — capture stderr into the file and propagate the exit to the executor. Do NOT silently continue.)
    - `bash .planning/phases/36-*/harness/capture_tools.sh > .planning/phases/36-*/evidence/codex_tools.txt 2>&1`
      (If capture_tools.sh exits 2 because the binary is missing, still capture the output and memo the user; do not fail the plan — Plan 02 consumers will need this failure information.)
    - Do NOT run bootstrap_test_project.sh in this plan — that's consumed by Plan 03.

    Commit: `chore(phase36-01): add wave-0 harness + baseline evidence`.
  </action>
  <acceptance_criteria>
    - `test -x .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/bootstrap_test_project.sh`
    - `bash -n .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/bootstrap_test_project.sh` exits 0
    - `grep -q '01-two-plan-phase' .planning/phases/36-*/harness/bootstrap_test_project.sh`
    - `grep -q 'A.txt' .planning/phases/36-*/harness/bootstrap_test_project.sh` and `grep -q 'B.txt' .planning/phases/36-*/harness/bootstrap_test_project.sh`
    - `test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/reproduction.txt`
    - `test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/codex_tools.txt`
    - `grep -qE 'spawn_agent' .planning/phases/36-*/evidence/reproduction.txt`
    - `test "$(grep -cE '^(exec_command|spawn_agent|apply_patch)$' .planning/phases/36-*/evidence/codex_tools.txt)" -ge 3` (addresses I2 — ensures strings capture actually produced real binary tool-name hits on their own lines, not just substring matches in help output)
    - `git log --oneline -1 | grep -q 'phase36-01'`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/reproduction.txt && test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/codex_tools.txt && grep -q 'spawn_agent' .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/reproduction.txt</automated>
  </verify>
  <done>Bootstrap script syntax-valid and executable; evidence files exist under evidence/ with spawn_agent mentions; atomic commit `chore(phase36-01): ...` present.</done>
</task>

</tasks>

<verification>
- `test -d .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness`
- `ls .planning/phases/36-*/harness/ | grep -E 'reproduce.sh|capture_tools.sh|bootstrap_test_project.sh' | wc -l` — expect 3
- `ls .planning/phases/36-*/evidence/ | wc -l` — expect ≥ 2 (reproduction.txt, codex_tools.txt)
- `bash -n .planning/phases/36-*/harness/reproduce.sh && bash -n .planning/phases/36-*/harness/capture_tools.sh && bash -n .planning/phases/36-*/harness/bootstrap_test_project.sh`
- `git log --oneline | head -5 | grep -q 'chore(phase36-01)'`
- Current branch is `sessions/codex-adapter-fix`, NOT `main`:
  `test "$(git rev-parse --abbrev-ref HEAD)" = "sessions/codex-adapter-fix"`
</verification>

<success_criteria>
- 3 harness scripts exist, all syntax-valid, all `chmod +x`.
- Baseline evidence captured (reproduction.txt shows current spawn_agent state, codex_tools.txt shows tool surface).
- Bootstrap script NOT yet executed (Plan 03 owns execution).
- One atomic commit on `sessions/codex-adapter-fix`. Zero pushes to `main`.
- STOP condition for "symptom not reproduced" encoded in reproduce.sh.
</success_criteria>

<output>
After completion, create `.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-01-SUMMARY.md`
documenting: observed spawn_agent count, Codex version, skill count, tool-name set found in the binary, and any anomalies
(missing binary path, nonzero spawn count triggering the STOP condition, etc.).
</output>
