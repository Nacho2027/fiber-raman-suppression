---
phase: 36
plan: 02
plan_id: 36-02
type: execute
wave: 1
depends_on: [36-01]
# Frontmatter split (per W7): in-repo artifacts go in files_modified;
# external-worktree artifacts (the fork at ~/src/gsd-fork/) are listed under
# external_artifacts for traceability but are NOT tracked by this repo's git.
files_modified:
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/lint_adapter.sh
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-install-cmd.sh
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-install-log.txt
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/adapter-lint.txt
external_artifacts:
  # Lives outside this worktree; committed in a separate repo under the user's GitHub account.
  - ~/src/gsd-fork/bin/lib/convertClaudeAgentToCodexAgent.* (generator location — grep determines)
  - ~/src/gsd-fork/skills/**/SKILL.md (regenerated output of the template change)
autonomous: false
requirements: []
requirements_addressed:
  - CTX-fork-workflow
  - CTX-three-fix-directions (Direction A locked)
  - CTX-research-first-methodology
user_setup:
  - service: github
    why: "Fork gsd-build/get-shit-done into the user's account for adapter edits"
    env_vars: []
    dashboard_config:
      - task: "Create fork of https://github.com/gsd-build/get-shit-done under user's GitHub account"
        location: "https://github.com/gsd-build/get-shit-done (Fork button, top-right)"

must_haves:
  truths:
    - "A fork of gsd-build/get-shit-done exists under the user's account with branch fix/codex-spawn-agent"
    - "The adapter generator has been located in the fork and edited so that emitted SKILL.md files carry the Direction A block"
    - "At least 80/81 regenerated skills contain the USER AUTHORIZATION NOTICE paragraph"
    - "The patched adapter is installed locally, overriding GSD 1.38.1 in ~/.codex/skills/"
    - "BLACKLIST_SKILLS in the generator is populated verbatim from CLAUDE.md Codex Runtime Constraints"
    - "The exact install command is preserved in a sourceable shell file so later plans can replay it deterministically"
  artifacts:
    - path: ~/src/gsd-fork/
      provides: "Working clone of the fork on branch fix/codex-spawn-agent"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt
      provides: "URL of the created fork + commit SHA of the adapter change"
      contains: "github.com"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-install-cmd.sh
      provides: "Sourceable shell file containing INSTALL_CMD=\"<cmd>\" on line 1 — replayable by later plans"
      contains: "INSTALL_CMD="
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-install-log.txt
      provides: "Captured stdout/stderr from the install invocation"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/lint_adapter.sh
      provides: "Static grep audit of every ~/.codex/skills/*/SKILL.md"
      contains: "USER AUTHORIZATION NOTICE"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/adapter-lint.txt
      provides: "Pass/fail count of patched skills"
  key_links:
    - from: Fork generator (bin/lib/convertClaudeAgentToCodexAgent.*)
      to: skills/**/SKILL.md (emitted output)
      via: Install step — `npm install -g file:$FORK_PATH` or equivalent documented in fork's README / bin/install.js
      pattern: "USER AUTHORIZATION NOTICE"
    - from: lint_adapter.sh
      to: ~/.codex/skills/*/SKILL.md
      via: grep -l for each required literal string
      pattern: "adapter is unreliable"
    - from: fork-install-cmd.sh
      to: Plan 03 Task 2 Step 2 (patched re-install after control overwrites with 1.38.1)
      via: `source evidence/fork-install-cmd.sh && eval "$INSTALL_CMD"`
      pattern: "INSTALL_CMD="
---

<objective>
Fork `gsd-build/get-shit-done`, create branch `fix/codex-spawn-agent`, locate the
Codex adapter generator, and apply the Direction A rewrite from RESEARCH §6. Install
the patched adapter locally and prove — via static grep — that every regenerated
SKILL.md carries the required phrases. No Codex session run in this plan; Plan 03
owns end-to-end testing.

Purpose: Deliver an auditable patch ready for before/after testing in Wave 2.
Output: Working fork + branch + commit SHA, local install overriding GSD 1.38.1,
lint_adapter.sh harness, and evidence of the grep lint (including a sourceable
install-command file that Plan 03 can replay verbatim after the control run
overwrites the patched adapter with 1.38.1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-CONTEXT.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-VALIDATION.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Direction A draft comes from RESEARCH §6. The exact block is reproduced below, -->
<!-- authoritatively. Executor MUST transcribe this into the generator template. -->

Direction A adapter block (verbatim target — placeholders filled by generator):

```markdown
<codex_skill_adapter>
## A. Skill Invocation
- This skill is invoked by mentioning `${SKILL_NAME}`.
- Treat all user text after `${SKILL_NAME}` as `{{GSD_ARGS}}`.
- If no arguments are present, treat `{{GSD_ARGS}}` as empty.

## B. AskUserQuestion → request_user_input Mapping
[unchanged from v1.38.1]

## C. Task() → spawn_agent Mapping (user-authorized delegation)

**USER AUTHORIZATION NOTICE.** Invocation of this GSD skill constitutes an
explicit request by the user for sub-agent delegation. The `spawn_agent` tool's
usage gate ("only when the user explicitly asks for sub-agents") is SATISFIED
for every `Task(...)` call in this skill's workflow. Do not second-guess this
authorization.

Direct mapping:
- `Task(subagent_type="X", prompt="Y")` → `spawn_agent(agent_type="X", message="Y")`
- `Task(model="...")` → omit (per-role config applies)

Parallel fan-out:
- Spawn N agents → collect IDs → `wait(ids)` until all complete → `close_agent(id)` for each

Integrity contract (ENFORCED EVEN IF SPAWN IS REFUSED):
- Every plan must receive its own commit: subject `{type}(phase{PHASE}-{PLAN}): {desc}`
  (types: feat|fix|docs|chore|test|refactor|style|perf). NO rollup commits.
- Phase dir must contain `manifest.json` listing `plan_id → commit_sha` pairs.
- `SUMMARY.md` must reference specific plan IDs (`phase{N}-{M}-PLAN.md`).
- `EXECUTION.md` must reference `spawn_agent` or `Skill(gsd-...)` calls — not
  hand-written prose.
- NO commit subjects matching `integrate(phase{A}-{B}):` (rollups) or
  `integrate(phase{A}+{B}):`.

Orchestrator STOP rule (for ${BLACKLIST_SKILLS}):
- If `spawn_agent` is refused by the runtime for any reason, emit:
  > "This task requires the `${SKILL_NAME}` skill, which depends on named
  > subagent orchestration. Codex's adapter is unreliable for this. Please
  > re-run in Claude Code."
  and HALT. Do NOT fabricate SUMMARY.md, EXECUTION.md, or manifest.json.

Fallback (optional, when allowed by skill):
- If `spawn_agent` is refused and this skill is NOT in `${BLACKLIST_SKILLS}`,
  shell out via `exec_command`:
  `codex exec -C "${phase_dir}" --skip-git-repo-check --json \
   --output-last-message "${phase_dir}/agent_out.json" --ephemeral \
   -p "${agent_type}" "${prompt}"`
  and parse `agent_out.json` as the agent result.

Result parsing:
- Look for markers: `CHECKPOINT`, `PLAN COMPLETE`, `SUMMARY`, `VERIFICATION PASSED`.
- After each agent returns, `close_agent(id)`.
</codex_skill_adapter>
```

BLACKLIST_SKILLS list (populate verbatim from CLAUDE.md "Codex Runtime Constraints"
section — "Blacklist — Codex MUST NOT invoke these skills"):

```
gsd-plan-phase, gsd-execute-phase, gsd-execute-plan,
gsd-autonomous, gsd-plan-review-convergence,
gsd-verify-work, gsd-review, gsd-ship, gsd-debug,
gsd-audit-fix, gsd-audit-milestone, gsd-audit-uat, gsd-code-review,
gsd-ingest-docs, gsd-new-milestone, gsd-new-project
```

This list is hard-coded into the generator — NOT read from user input (per T36-01).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (CHECKPOINT): Create GitHub fork + clone + branch</name>
  <read_first>
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-CONTEXT.md (§ "Git hygiene" locked decisions)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md (§7 Upstream Status)
  </read_first>
  <files>.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt, ~/src/gsd-fork/ (external clone)</files>
  <what-built>Nothing automated yet — this checkpoint creates the external artifact the rest of the plan depends on.</what-built>
  <how-to-verify>
    Executor MUST NOT silently skip this step. Do one of:
    (A) Use `gh` CLI if authenticated: `gh repo fork gsd-build/get-shit-done --clone=false` then `gh repo clone <user>/get-shit-done ~/src/gsd-fork`.
    (B) If `gh` is not authenticated, ASK the user to create the fork at https://github.com/gsd-build/get-shit-done (click "Fork") and paste the fork URL back.

    Then:
    1. `cd ~/src/gsd-fork && git checkout -b fix/codex-spawn-agent`.
    2. Record the fork URL and branch name in
       `.planning/phases/36-*/evidence/fork-url.txt` in this worktree.

    STOP condition: If neither path works (no gh auth, no user response after 10 min),
    halt and escalate — do NOT push a patched adapter into the wrong repo.
  </how-to-verify>
  <action>
    Create the fork of gsd-build/get-shit-done under the user's account (via `gh repo fork gsd-build/get-shit-done --clone=false` if `gh` is authenticated, else ask the user and paste URL back), clone to `~/src/gsd-fork`, and checkout branch `fix/codex-spawn-agent`. Write the fork URL and branch name into `.planning/phases/36-*/evidence/fork-url.txt` in this worktree. Do NOT commit the external clone into this repo. HALT if neither the gh path nor the user-paste path succeeds — do NOT proceed to Task 2 without a confirmed fork.
  </action>
  <verify>
    <automated>test -d ~/src/gsd-fork/.git && test "$(git -C ~/src/gsd-fork rev-parse --abbrev-ref HEAD)" = "fix/codex-spawn-agent" && test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt && grep -qE 'github.com/[^/]+/get-shit-done' .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt</automated>
  </verify>
  <done>Fork exists under user's account, local clone at ~/src/gsd-fork on branch fix/codex-spawn-agent, fork URL recorded in evidence/fork-url.txt.</done>
  <resume-signal>Once the fork exists, branch is checked out, and evidence/fork-url.txt contains the fork URL + branch name, the executor may proceed.</resume-signal>
  <acceptance_criteria>
    - `test -d ~/src/gsd-fork/.git`
    - `git -C ~/src/gsd-fork rev-parse --abbrev-ref HEAD` returns `fix/codex-spawn-agent`
    - `git -C ~/src/gsd-fork remote -v | grep -q 'github.com'` — fork URL present as origin
    - `test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt`
    - `grep -qE 'github.com/[^/]+/get-shit-done' .planning/phases/36-*/evidence/fork-url.txt`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Locate the adapter generator and apply Direction A rewrite</name>
  <read_first>
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md (§6 Draft Replacement block — the <interfaces> block above reproduces the target text verbatim)
    - ~/src/gsd-fork/ (entire fork, especially bin/lib/ and any install.js / install.ts)
    - CLAUDE.md section "Codex Runtime Constraints" for the exact BLACKLIST_SKILLS list
  </read_first>
  <files>
    ~/src/gsd-fork/bin/lib/convertClaudeAgentToCodexAgent.* (or wherever the generator actually lives),
    ~/src/gsd-fork/skills/**/SKILL.md (regenerated output — if generator writes to disk in the fork repo)
  </files>
  <action>
    Step 1 — Locate the generator:
    ```bash
    cd ~/src/gsd-fork
    grep -rn 'codex_skill_adapter\|spawn_agent\|Task() → spawn_agent' bin/ src/ lib/ 2>/dev/null | head -40
    ```
    The expected file per PR #791 is `bin/lib/convertClaudeAgentToCodexAgent.{ts,js,mjs,cjs}`.
    If grep resolves to a different file, use whatever it finds. The file that contains the
    *template text* of the adapter block is the target.

    **STOP condition 2:** If after a 10-minute search (grep + ls + git log -p for the PR #791 merge commit in the fork) no file emerges, halt, write `.planning/phases/36-*/evidence/generator-not-found.txt` summarizing where you searched, and escalate to the user. Do NOT guess and patch the wrong file.

    Step 2 — Apply the Direction A rewrite:
    Replace the Section C template in the generator with the full block from the `<interfaces>`
    section above (reproduced from RESEARCH §6). Sections A and B are unchanged.

    The generator MUST:
    - Hard-code the BLACKLIST_SKILLS list (per T36-01) — add it as a module-level const
      array in the generator, not read from user input or env.
    - Substitute `${SKILL_NAME}` with the skill's own name when emitting each SKILL.md.
    - Interpolate BLACKLIST_SKILLS into the STOP rule text by listing them explicitly or
      by leaving the literal string `${BLACKLIST_SKILLS}` and emitting a resolved list
      elsewhere in the skill — whichever matches the existing generator's templating style.
    - Include the Fallback subsection with `codex exec -C "${phase_dir}"` verbatim — this
      enforces T36-02 (scoping the shell-out fallback to the phase directory).

    Step 3 — Regenerate skills in the fork:
    If the fork ships a `skills/` tree that is regenerated by the build (check README or
    package.json scripts: likely `npm run build` or `node bin/build-skills.js`), run that
    command. If skills/*/SKILL.md are only produced at install time, skip this step and
    rely on Task 3's install to emit them into ~/.codex/skills/.

    Step 4 — Commit:
    ```bash
    cd ~/src/gsd-fork
    git add bin/ src/ lib/ skills/  # whatever the actual paths are
    git commit -m "fix(codex-adapter): add USER AUTHORIZATION NOTICE, integrity contract, STOP rule for blacklisted orchestrators"
    ```
    Record commit SHA in `.planning/phases/36-*/evidence/fork-url.txt` (append).
  </action>
  <acceptance_criteria>
    - Generator file identified: `ls ~/src/gsd-fork/bin/lib/convertClaudeAgentToCodexAgent.* 2>/dev/null || find ~/src/gsd-fork -type f \( -name '*.js' -o -name '*.ts' -o -name '*.mjs' -o -name '*.cjs' \) -exec grep -l 'codex_skill_adapter' {} \;` returns ≥ 1 path. Record that path as `<generator-file>` for the remaining checks below.
    - `grep -q 'USER AUTHORIZATION NOTICE' <generator-file>` — target literal present in template.
    - `grep -q 'adapter is unreliable' <generator-file>` — STOP rule present.
    - `grep -q 'manifest.json' <generator-file>` — integrity contract present.
    - `grep -q 'phase{PHASE}-{PLAN}' <generator-file>` — atomic-commit template present.
    - `grep -qE "gsd-plan-phase.*gsd-execute-phase|BLACKLIST_SKILLS.*=.*\\[" <generator-file>` — blacklist hard-coded.
    - **T36-02 mitigation enforcement (addresses W2)** — fallback is scoped AND present:
      - `grep -q '\-C "\${phase_dir}"' <generator-file>` — ensures the `codex exec` fallback is scoped to the phase directory, never to `$HOME` or an ancestor.
      - `grep -q 'codex exec' <generator-file>` — ensures the shell-out fallback pattern is present in the template.
    - `git -C ~/src/gsd-fork log --oneline -1 | grep -q 'fix(codex-adapter)'` — commit landed on the branch.
    - `grep -qE '[a-f0-9]{7,40}' .planning/phases/36-*/evidence/fork-url.txt` — commit SHA recorded.
  </acceptance_criteria>
  <verify>
    <automated>cd ~/src/gsd-fork && git log --oneline -1 | grep -q 'fix(codex-adapter)' && grep -rq 'USER AUTHORIZATION NOTICE' bin/ src/ lib/ 2>/dev/null && grep -rq -- '-C "\${phase_dir}"' bin/ src/ lib/ 2>/dev/null</automated>
  </verify>
  <done>Generator located, Direction A block transcribed, BLACKLIST_SKILLS hard-coded, `codex exec -C "${phase_dir}"` fallback present (T36-02 enforced), atomic commit on `fix/codex-spawn-agent`, SHA recorded in evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Install patched adapter locally + write lint_adapter.sh + run the lint</name>
  <read_first>
    - ~/src/gsd-fork/README.md (install instructions)
    - ~/src/gsd-fork/bin/install.js or ~/src/gsd-fork/package.json scripts block — to discover the exact install command
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt (fork path from Task 1)
  </read_first>
  <files>
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/lint_adapter.sh,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-install-cmd.sh,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-install-log.txt,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/adapter-lint.txt
  </files>
  <action>
    Step 1 — Determine the install command:
    The fork may expose install via any of (check README + package.json):
    - `cd ~/src/gsd-fork && npm install -g .` (npm global from local path)
    - `cd ~/src/gsd-fork && node bin/install.js codex` (if gsd ships its own installer)
    - `cd ~/src/gsd-fork && npx . install codex` (npx invocation)
    - `cd ~/src/gsd-fork && ./bin/install.sh --target codex` (if shell-based)

    Pick the one the fork actually supports.

    Step 2 — Persist the install command as a sourceable shell file (**addresses B2 —
    supersedes the previous brittle grep-recovery approach**):

    Write `evidence/fork-install-cmd.sh` so that line 1 is literally
    `INSTALL_CMD="<the exact command>"` (double quotes around the whole command;
    escape any inner double quotes). No other lines required, but an optional
    `# Captured by phase36-02 on <date>` comment above the assignment is allowed.
    Then `chmod +x evidence/fork-install-cmd.sh`.

    Self-test the file before proceeding:
    ```bash
    bash -c 'source .planning/phases/36-*/evidence/fork-install-cmd.sh && test -n "$INSTALL_CMD" && echo "OK: $INSTALL_CMD"'
    ```
    If this fails, fix the file before running the install — Plan 03 will replay it
    exactly this way, and a broken file silently no-ops the patched re-install.

    Step 3 — Run the install, capturing full stdout/stderr separately from the command file:
    ```bash
    source .planning/phases/36-*/evidence/fork-install-cmd.sh
    eval "$INSTALL_CMD" > .planning/phases/36-*/evidence/fork-install-log.txt 2>&1
    ```

    Step 4 — Write `harness/lint_adapter.sh` (bash, set -euo pipefail) that:
    1. Iterates `for skill in ~/.codex/skills/*/SKILL.md; do …`.
    2. For each skill, checks that ALL 5 literals are present (from Direction A):
       - `USER AUTHORIZATION NOTICE`
       - `phase{PHASE}-{PLAN}` (literal template form)
       - `manifest.json`
       - `spawn_agent(agent_type=`
       - For skills whose name matches any entry in the BLACKLIST list (case-insensitive grep against the skill's parent dir name), additionally require `adapter is unreliable`.
    3. For each skill, prints `PASS:` or `FAIL: <missing-literal>` on its own line.
    4. At the end, prints totals: `Total: N, Pass: P, Fail: F` and exits 0 iff `F == 0`.

    Make it chmod +x.

    Step 5 — Run the lint and capture:
    ```bash
    bash .planning/phases/36-*/harness/lint_adapter.sh > .planning/phases/36-*/evidence/adapter-lint.txt 2>&1 \
      || echo "LINT EXITED NONZERO — see file for details" >> .planning/phases/36-*/evidence/adapter-lint.txt
    ```

    Step 6 — Per-BLACKLIST-skill STOP-phrase coverage (**addresses W3 — explicit per-skill
    gate, not just aggregate**). Run this block and fail if ANY skill on the hard-coded
    list is installed but missing the STOP phrase:
    ```bash
    missing=0
    for s in gsd-plan-phase gsd-execute-phase gsd-execute-plan gsd-autonomous \
             gsd-plan-review-convergence gsd-verify-work gsd-review gsd-ship gsd-debug \
             gsd-audit-fix gsd-audit-milestone gsd-audit-uat gsd-code-review \
             gsd-ingest-docs gsd-new-milestone gsd-new-project; do
      [ -f "$HOME/.codex/skills/$s/SKILL.md" ] || continue  # some skills (e.g., gsd-plan-review-convergence) may not be installed
      if ! grep -q 'adapter is unreliable' "$HOME/.codex/skills/$s/SKILL.md"; then
        echo "MISSING STOP phrase in $s" | tee -a .planning/phases/36-*/evidence/adapter-lint.txt
        missing=$((missing + 1))
      fi
    done
    test "$missing" -eq 0 || { echo "FAIL: $missing blacklist skill(s) missing STOP phrase — do NOT advance to Plan 03" >&2; exit 1; }
    ```

    Expectation: At least 80/81 skills PASS the general lint, AND every installed
    BLACKLIST skill carries the STOP phrase. If either fails, fix the generator and
    re-run Task 2 + Task 3 — Plan 03 MUST NOT start.

    Step 7 — Commit (in THIS repo, on sessions/codex-adapter-fix). Reference to Task 3's
    install (**W1 fix — previously said "Task 4's install"**):
    ```bash
    git add .planning/phases/36-*/harness/lint_adapter.sh \
            .planning/phases/36-*/evidence/fork-install-cmd.sh \
            .planning/phases/36-*/evidence/fork-install-log.txt \
            .planning/phases/36-*/evidence/adapter-lint.txt
    git commit -m "chore(phase36-02): install patched fork adapter (Task 3's install) + lint 81 skills"
    ```
  </action>
  <acceptance_criteria>
    - `test -x .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/harness/lint_adapter.sh`
    - `bash -n .planning/phases/36-*/harness/lint_adapter.sh` exits 0
    - `grep -q 'USER AUTHORIZATION NOTICE' .planning/phases/36-*/harness/lint_adapter.sh`
    - `grep -q 'adapter is unreliable' .planning/phases/36-*/harness/lint_adapter.sh`
    - **B2 fix — install command is deterministically replayable**:
      - `test -s .planning/phases/36-*/evidence/fork-install-cmd.sh`
      - `test -x .planning/phases/36-*/evidence/fork-install-cmd.sh`
      - `bash -c 'source .planning/phases/36-*/evidence/fork-install-cmd.sh && test -n "$INSTALL_CMD"'` — the sourceable file defines INSTALL_CMD
      - `head -1 .planning/phases/36-*/evidence/fork-install-cmd.sh | grep -qE '^(#.*|INSTALL_CMD=")'` — first non-empty line is either a comment or the assignment
      - `test -f .planning/phases/36-*/evidence/fork-install-log.txt` — separate log file captured
    - `test -f .planning/phases/36-*/evidence/adapter-lint.txt`
    - `grep -cE '^PASS:' .planning/phases/36-*/evidence/adapter-lint.txt` — expected ≥ 80
    - `grep -cE '^FAIL:' .planning/phases/36-*/evidence/adapter-lint.txt` — expected ≤ 1
    - Installed skills contain the new text:
      `grep -l 'USER AUTHORIZATION NOTICE' ~/.codex/skills/*/SKILL.md | wc -l` — expected ≥ 80
    - **W3 fix — per-BLACKLIST-skill STOP-phrase coverage**: the explicit `for s in <16 skills> ...` loop from Step 6 exits 0 (no "MISSING STOP phrase" lines appended to adapter-lint.txt for any installed blacklist skill).
      Verify with: `! grep -q 'MISSING STOP phrase' .planning/phases/36-*/evidence/adapter-lint.txt`
    - `git log --oneline -1 | grep -q 'chore(phase36-02)'`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/adapter-lint.txt && test "$(grep -l 'USER AUTHORIZATION NOTICE' ~/.codex/skills/*/SKILL.md | wc -l)" -ge 80 && bash -c 'source .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-install-cmd.sh && test -n "$INSTALL_CMD"' && ! grep -q 'MISSING STOP phrase' .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/adapter-lint.txt</automated>
  </verify>
  <done>Patched skills are live in ~/.codex/skills/, ≥80 pass the general lint, EVERY installed BLACKLIST skill carries the STOP phrase, install command preserved in sourceable `evidence/fork-install-cmd.sh`, atomic commit `chore(phase36-02)` on `sessions/codex-adapter-fix`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator → emitted SKILL.md | Template substitution; any placeholder fed from user input could inject arbitrary text into 81 skills |
| `codex exec` fallback → child Codex session | Child inherits filesystem + network access; scope of child controlled by `-C` target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T36-01 | Tampering / Injection | BLACKLIST_SKILLS placeholder in the adapter generator | mitigate | Hard-code BLACKLIST_SKILLS as a module-level const in the generator (Task 2, acceptance criterion `BLACKLIST_SKILLS.*=.*\[`); never source it from user input, CLI args, or environment variables. |
| T36-02 | Elevation of Privilege | `codex exec` shell-out fallback in Section C | mitigate | Always pass `-C "${phase_dir}"` (same directory as the orchestrator) in the fallback command — never an ancestor, never `$HOME`, never unset. Task 2 acceptance criteria now include BOTH `grep -q '\-C "\${phase_dir}"' <generator-file>` (fallback scoped) AND `grep -q 'codex exec' <generator-file>` (fallback present). Without both, Task 2 fails. |
</threat_model>

<verification>
- `git -C ~/src/gsd-fork rev-parse --abbrev-ref HEAD` = `fix/codex-spawn-agent`
- `git -C ~/src/gsd-fork log --oneline -1 | grep -q 'fix(codex-adapter)'`
- `grep -l 'USER AUTHORIZATION NOTICE' ~/.codex/skills/*/SKILL.md | wc -l` ≥ 80
- `grep -c '^PASS:' .planning/phases/36-*/evidence/adapter-lint.txt` ≥ 80
- Every installed BLACKLIST skill carries the STOP phrase: `! grep -q 'MISSING STOP phrase' .planning/phases/36-*/evidence/adapter-lint.txt`
- Install command is sourceable: `bash -c 'source .planning/phases/36-*/evidence/fork-install-cmd.sh && test -n "$INSTALL_CMD"'`
- `test "$(git rev-parse --abbrev-ref HEAD)" = "sessions/codex-adapter-fix"` (this worktree is still on its session branch)
- No pushes to `main` in either repo: `git -C ~/src/gsd-fork log origin/main --oneline | head -1` unchanged from pre-plan; this worktree likewise.
</verification>

<success_criteria>
- Fork exists, branch `fix/codex-spawn-agent` checked out, one atomic commit on it with the adapter rewrite.
- Direction A block present verbatim in the generator; BLACKLIST_SKILLS hard-coded; `codex exec -C "${phase_dir}"` fallback scoped (T36-02).
- Patched adapter installed; ≥80/81 `~/.codex/skills/*/SKILL.md` contain the new text.
- EVERY installed BLACKLIST skill carries the "adapter is unreliable" STOP phrase (W3).
- Install command preserved in sourceable `evidence/fork-install-cmd.sh` so Plan 03 can replay it deterministically (B2).
- `lint_adapter.sh` harness exists and its output is recorded under evidence/.
- One atomic commit on `sessions/codex-adapter-fix` in THIS repo; zero pushes to `main` in either repo.
- Threat model mitigations visible in the generator (hard-coded blacklist, scoped `-C` in fallback).
</success_criteria>

<output>
After completion, create `.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-02-SUMMARY.md`
documenting: fork URL, branch name, commit SHA in the fork, generator filename, install command
executed (cite `evidence/fork-install-cmd.sh`), and lint pass/fail counts (general + per-BLACKLIST).
Note any skill that legitimately lacks an adapter block.
</output>
