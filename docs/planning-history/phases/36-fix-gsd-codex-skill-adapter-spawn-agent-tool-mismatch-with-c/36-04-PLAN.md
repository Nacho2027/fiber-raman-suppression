---
phase: 36
plan: 04
plan_id: 36-04
type: execute
wave: 3
depends_on: [36-03]
files_modified:
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/contributing-live.md
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/upstream-labels.txt
  - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/upstream-labels-changed.md (only if label renamed upstream)
  - .planning/sessions/codex-adapter-fix-status.md
autonomous: false
requirements: []
requirements_addressed:
  - CTX-upstream-filing-gate
  - CTX-session-status-summary
user_setup:
  - service: github
    why: "File bug report against gsd-build/get-shit-done under user's account"
    env_vars: []
    dashboard_config:
      - task: "Submit issue with body from upstream/bug-report.md"
        location: "https://github.com/gsd-build/get-shit-done/issues/new/choose"

must_haves:
  truths:
    - "The live upstream CONTRIBUTING.md is fetched at filing time and cached in evidence/contributing-live.md, with a normalized label list in evidence/upstream-labels.txt"
    - "The operative label gate for BUG PRs resolves to `confirmed-bug` OR a rename memo is written to evidence/upstream-labels-changed.md with the new label name cited from the live CONTRIBUTING.md"
    - "A bug-report draft is written in upstream/bug-report.md covering reproduction, root cause, skills inventory, proposed fix (Direction A prose), and before/after evidence, citing the resolved bug-PR label with the live CONTRIBUTING.md URL + fetch timestamp"
    - "The draft is ≥ 500 words and cites PR #791 + v1.38.1 CHANGELOG check result"
    - "The user has filed the issue and pasted the URL back; the URL is recorded in sessions/codex-adapter-fix-status.md. Or the user has explicitly confirmed a defer with a dated confirmation line"
    - "The session-status summary is ≤ 500 words, records the resolved bug-PR label gate with the CONTRIBUTING.md URL and fetch timestamp, and covers root cause, fix, evidence, upstream URL, and gotchas"
  artifacts:
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/contributing-live.md
      provides: "Snapshot of the live upstream CONTRIBUTING.md fetched at filing time"
      contains: "label"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/upstream-labels.txt
      provides: "Normalized list of upstream label tokens matching the known patterns (confirmed-bug, approved-feature, bug-confirmed, feature-approved, ...)"
    - path: .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md
      provides: "Upstream bug-report body ready to paste into the GitHub issue form"
      contains: "spawn_agent"
      min_lines: 100
    - path: .planning/sessions/codex-adapter-fix-status.md
      provides: "Session closeout status: root cause, fix, evidence, upstream URL, gotchas (≤ 500 words). Records the resolved bug-PR label gate with CONTRIBUTING.md URL + fetch timestamp."
      contains: "https://github.com/gsd-build/get-shit-done/issues/"
  key_links:
    - from: upstream/bug-report.md
      to: evidence/control/ + evidence/patched/
      via: side-by-side before/after evidence table copied from 36-03-SUMMARY.md
      pattern: "control.*patched|before.*after"
    - from: upstream/bug-report.md
      to: evidence/upstream-labels.txt + evidence/contributing-live.md
      via: live-fetch-at-filing-time label resolution
      pattern: "confirmed-bug|approved-feature|bug-confirmed|feature-approved"
    - from: .planning/sessions/codex-adapter-fix-status.md
      to: GitHub issue URL
      via: executor-run manual filing step
      pattern: "github.com/gsd-build/get-shit-done/issues/[0-9]+"
---

<objective>
Draft the upstream bug report, wait on the user to file it, then write the ≤500-word
session closeout. This plan is deliberately non-autonomous at the filing step (CONTEXT
locks "file bug report FIRST, do NOT open PR before `confirmed-bug` label").

Per B1 (checker iter-1): CONTEXT.md now clarifies that upstream CONTRIBUTING.md has
TWO label gates — `confirmed-bug` for BUG PRs and `approved-feature` for FEATURE PRs.
Because this phase files a BUG, the operative gate is `confirmed-bug`. But label
names may have been renamed upstream since research, so this plan's first step is
a live curl of CONTRIBUTING.md with a union grep that accepts the known variants
(confirmed-bug | approved-feature | bug-confirmed | feature-approved) and records
whichever is actually present. If none are, a revision memo is written — the plan
never assumes the label name blindly.

Purpose: Convert the patched-run evidence into a high-quality upstream ticket, and
record what happened in a session-status file the user can scan in 2 minutes.
Output: `upstream/bug-report.md` (≥ 500 words, ~150+ lines), `evidence/contributing-live.md` + `evidence/upstream-labels.txt` (live-fetch snapshot), `.planning/sessions/codex-adapter-fix-status.md` (≤ 500 words).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-CONTEXT.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-VALIDATION.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-01-SUMMARY.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-02-SUMMARY.md
@.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-03-SUMMARY.md

<interfaces>
<!-- Bug-report body sections, per gsd-build CONTRIBUTING.md bug flow and RESEARCH §7 -->

Required sections (in this order):

1. **Title** (issue title field): "Codex adapter: `spawn_agent` invocation count = 0 across 17k+ log rows — base-instruction gate blocks delegation silently"

2. **Reproduction**
   - sqlite query (verbatim from RESEARCH §2): `sqlite3 ~/.codex/logs_*.sqlite "SELECT COUNT(*) FROM logs WHERE feedback_log_body LIKE '%tool_name=\"spawn_agent\"%';"` → 0
   - Plan 01 `evidence/reproduction.txt` values (spawn_agent count, total rows, observed tool_names).

3. **Root Cause**
   - Quote the base-instruction gate text from the native binary (RESEARCH §1): *"Only use `spawn_agent` if and only if the user explicitly asks for sub-agents, delegation, or parallel agent work. Requests for depth, thoroughness, research, investigation, or detailed codebase analysis do not count as permission to spawn."*
   - Explain that GSD's `<codex_skill_adapter>` block does not establish user authorization, so the model falls back to `exec_command`.

4. **Affected Skills Inventory** — copy the table from RESEARCH §4 (Critical/High/Medium/Low rows with counts).

5. **Proposed Fix (Direction A, PROSE only — no diff yet)**
   - Explicit USER AUTHORIZATION NOTICE at Section C.
   - Integrity contract baked into the skill text (atomic commits, manifest.json, plan-ID-referenced SUMMARY/EXECUTION).
   - STOP rule for orchestrator skills when spawn_agent still refuses.
   - Optional `codex exec` shell-out fallback for non-blacklisted skills.
   - Rationale: attacking the prompt-level gate rather than swapping tool names (Direction B = homogeneous CSV, inadequate; Direction C = full process-level shell-out, too disruptive as primary).

6. **Before/After Evidence Table** — direct copy from `36-03-SUMMARY.md`, formatted as:

   | Criterion | Control (1.38.1) | Patched (fork) |
   |---|---|---|
   | spawn_agent invocations (5-min window) | {value from evidence/control/spawn_count.txt} | {value from evidence/patched/spawn_count.txt} |
   | Atomic per-plan commits | {count} | {count} |
   | Rollup commits | {count} | 0 |
   | manifest.json present | {YES/NO} | YES |
   | check-phase-integrity.sh exit | {exit} | 0 |
   | SUMMARY references plan IDs | {hits} | ≥ 2 |
   | EXECUTION mentions subagent | {hits} | ≥ 1 |

7. **References**
   - PR #791 (merged 2026-02-28 by @trek-e) introduced the mapping without runtime verification.
   - v1.38.1 CHANGELOG: no adapter changes since PR #791 (from RESEARCH §7). If a newer version shipped between 2026-04-20 and filing date, Plan 04 executor must re-check and note it.
   - developers.openai.com/codex/subagents — documents `spawn_agents_on_csv` only; `spawn_agent` exists in binary, undocumented.
   - **Live CONTRIBUTING.md fetch result** (from evidence/contributing-live.md + evidence/upstream-labels.txt):
     cite the exact bug-PR label token resolved at filing time
     (`confirmed-bug` per CONTEXT.md, or a rename recorded in evidence/upstream-labels-changed.md)
     with the CONTRIBUTING.md URL and fetch timestamp.

8. **Offer to PR**
   - "Happy to open a PR against the fork at <fork-URL> (branch `fix/codex-spawn-agent`, commit {SHA from evidence/fork-url.txt}) once the `<resolved-bug-label>` label is applied, per CONTRIBUTING.md (fetched <timestamp>, see evidence/contributing-live.md)."

Session-status file layout (`.planning/sessions/codex-adapter-fix-status.md`, ≤ 500 words):
```
# Session: codex-adapter-fix (Phase 36)

**Worktree:** ~/raman-wt-codex-adapter (branch sessions/codex-adapter-fix)
**Fork:** <URL from evidence/fork-url.txt> (branch fix/codex-spawn-agent, commit <SHA>)
**Upstream issue:** <URL the user pastes back>
**Bug-PR label gate:** <resolved token> (live-fetched from <CONTRIBUTING.md URL> at <ISO timestamp>, see evidence/upstream-labels.txt)

## Root cause
<2-3 sentences — base-instruction gate blocks spawn_agent despite correct wiring>

## Fix
<2-3 sentences — Direction A adapter rewrite + BLACKLIST STOP rule>

## Evidence
<2-3 bullets — patched run passes all 7 criteria, control fails N of 7, spawn_count 0 → ≥ 1>

## Gotchas for the next agent
<3-5 bullets — install command, where the generator lives, the STOP conditions>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-fetch CONTRIBUTING.md, resolve bug-PR label, draft upstream/bug-report.md</name>
  <read_first>
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-CONTEXT.md (§ "Upstream filing gate" — clarifies that `confirmed-bug` gates BUG PRs and `approved-feature` gates FEATURE PRs; this phase files a BUG so the operative gate is `confirmed-bug`)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md (§2, §3, §4, §5, §7 — all cited in the report body)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-03-SUMMARY.md (before/after table values)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/control/spawn_count.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/control/git_log.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/control/integrity.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/patched/spawn_count.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/patched/git_log.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/patched/integrity.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt (fork URL + SHA)
  </read_first>
  <files>
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/contributing-live.md,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/upstream-labels.txt,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/upstream-labels-changed.md,
    .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md
  </files>
  <action>
    Step 0 — **B1: live-fetch CONTRIBUTING.md and resolve the operative bug-PR label** (NEW
    step, runs before any writing of bug-report.md):
    ```bash
    mkdir -p .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence
    FETCH_TS="$(date -u +%FT%TZ)"
    CONTRIB_URL="https://raw.githubusercontent.com/gsd-build/get-shit-done/main/CONTRIBUTING.md"
    curl -sL "$CONTRIB_URL" \
      -o .planning/phases/36-*/evidence/contributing-live.md
    test -s .planning/phases/36-*/evidence/contributing-live.md \
      || { echo "ABORT: empty CONTRIBUTING.md fetch from $CONTRIB_URL at $FETCH_TS" >&2; exit 2; }

    # Normalize — capture every token matching any of the known label variants.
    grep -oE '(confirmed-bug|approved-feature|bug-confirmed|feature-approved)[A-Za-z0-9_-]*' \
      .planning/phases/36-*/evidence/contributing-live.md \
      | sort -u > .planning/phases/36-*/evidence/upstream-labels.txt

    test -s .planning/phases/36-*/evidence/upstream-labels.txt \
      || { echo "ABORT: no known label tokens found in CONTRIBUTING.md — upstream may have renamed labels entirely. Halt and escalate." >&2; exit 2; }

    # Resolve the operative bug-PR label:
    # - Prefer `confirmed-bug` (per CONTEXT.md).
    # - Accept `bug-confirmed` as a plausible rename.
    # - Otherwise record a rename memo and continue with the first token as fallback.
    if grep -qx 'confirmed-bug' .planning/phases/36-*/evidence/upstream-labels.txt; then
      RESOLVED_BUG_LABEL='confirmed-bug'
    elif grep -qx 'bug-confirmed' .planning/phases/36-*/evidence/upstream-labels.txt; then
      RESOLVED_BUG_LABEL='bug-confirmed'
    else
      RESOLVED_BUG_LABEL="$(head -1 .planning/phases/36-*/evidence/upstream-labels.txt)"
      cat > .planning/phases/36-*/evidence/upstream-labels-changed.md <<EOF
# Upstream label rename detected

**Fetched:** $FETCH_TS
**Source:** $CONTRIB_URL
**Resolved bug-PR label (fallback — first matching token):** \`$RESOLVED_BUG_LABEL\`

The expected bug-PR label (\`confirmed-bug\`) was NOT found in the live CONTRIBUTING.md.
Known variants observed:
\`\`\`
$(cat .planning/phases/36-*/evidence/upstream-labels.txt)
\`\`\`

Bug-report and session-status will cite \`$RESOLVED_BUG_LABEL\` as the operative gate.
EOF
    fi

    # Export for subsequent steps in this task:
    echo "$RESOLVED_BUG_LABEL" > .planning/phases/36-*/evidence/resolved-bug-label.txt
    echo "$FETCH_TS" > .planning/phases/36-*/evidence/contributing-fetch-ts.txt
    echo "$CONTRIB_URL" > .planning/phases/36-*/evidence/contributing-url.txt
    ```

    Step 1 — Create directory `.planning/phases/36-*/upstream/` and write `bug-report.md`
    with the 8 sections from the `<interfaces>` block above. Fill every value from the
    listed evidence files — no `{TBD}` placeholders allowed in the final draft.

    Explicit content requirements:
    - Target length: ≥ 500 words. Use tables and fenced code blocks to pad naturally if needed;
      do NOT fluff with filler paragraphs.
    - Include verbatim quote of the base-instruction gate text (RESEARCH §1).
    - Include the full affected-skills table from RESEARCH §4 (all rows, severity column intact).
    - Include the 7-criterion before/after table with actual numeric values pulled from evidence/.
    - Reference PR #791 by number and author; reference CHANGELOG check (note current Codex
      version + current GSD version at filing time — re-check by running `codex --version` and
      `cat ~/.codex/get-shit-done/VERSION` inside the task and echoing into the report).
    - **B1 — Cite the resolved bug-PR label**. In Section 7 (References) and Section 8 (Offer
      to PR), substitute `$(cat .planning/phases/36-*/evidence/resolved-bug-label.txt)` for the
      label name, and include a parenthetical citation of the form:
      `(per CONTRIBUTING.md, fetched <timestamp> from <URL>, see evidence/contributing-live.md)`
      using the values in `evidence/contributing-fetch-ts.txt` and `evidence/contributing-url.txt`.
    - Offer a PR against the fork URL + commit SHA from `evidence/fork-url.txt`.
    - Do NOT include a diff or patch file — CONTRIBUTING.md wants prose at the bug-report stage.

    Refresh check — the second STOP condition from CONTEXT escape hatches:
    - Before finalizing, run `curl -s https://api.github.com/repos/gsd-build/get-shit-done/releases/latest | grep -E '"tag_name"'` and compare to 1.38.1. If a newer release exists, add a note to Section 7:
      "Re-verified against {current tag} on {date}; adapter text in that release still contains the boilerplate documented above." Include the grep/diff evidence or halt and escalate if the upstream has meaningfully changed.

    Commit: `docs(phase36-04): draft upstream bug report + live CONTRIBUTING fetch`.
  </action>
  <acceptance_criteria>
    - **B1 — live-fetch artifacts exist**:
      - `test -s .planning/phases/36-*/evidence/contributing-live.md`
      - `test -s .planning/phases/36-*/evidence/upstream-labels.txt`
      - `test -f .planning/phases/36-*/evidence/resolved-bug-label.txt`
      - `test -f .planning/phases/36-*/evidence/contributing-fetch-ts.txt`
      - `test -f .planning/phases/36-*/evidence/contributing-url.txt`
    - **B1 — resolved label is either `confirmed-bug` or a rename memo exists**:
      `grep -qx 'confirmed-bug' .planning/phases/36-*/evidence/resolved-bug-label.txt \
        || test -f .planning/phases/36-*/evidence/upstream-labels-changed.md`
    - **I1 — acceptance greps use the union pattern so a rename does not silently break the check**:
      `grep -qiE 'confirmed-bug|approved-feature|bug-confirmed|feature-approved' .planning/phases/36-*/upstream/bug-report.md`
    - `test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md`
    - Word count ≥ 500: `test "$(wc -w < .planning/phases/36-*/upstream/bug-report.md)" -ge 500`
    - Contains all 8 required section markers:
      `grep -qE '^#.*Reproduction' .planning/phases/36-*/upstream/bug-report.md &&
       grep -qE '^#.*Root Cause' .planning/phases/36-*/upstream/bug-report.md &&
       grep -qiE 'affected skills' .planning/phases/36-*/upstream/bug-report.md &&
       grep -qiE 'proposed fix|direction a' .planning/phases/36-*/upstream/bug-report.md &&
       grep -qiE 'before/after|evidence' .planning/phases/36-*/upstream/bug-report.md &&
       grep -qE '#791' .planning/phases/36-*/upstream/bug-report.md`
    - Bug-report cites the CONTRIBUTING.md URL + fetch timestamp:
      `grep -qE 'raw\\.githubusercontent\\.com/gsd-build/get-shit-done' .planning/phases/36-*/upstream/bug-report.md`
      AND `grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' .planning/phases/36-*/upstream/bug-report.md`
    - Base-instruction gate quoted verbatim: `grep -q 'Only use .spawn_agent. if and only if the user explicitly asks for sub-agents' .planning/phases/36-*/upstream/bug-report.md`
    - Numeric evidence present (no TBD): `! grep -qE '\{TBD\}|TODO|\{\{value\}\}' .planning/phases/36-*/upstream/bug-report.md`
    - Fork URL + commit SHA cited: `grep -qE 'github.com/[^/]+/get-shit-done' .planning/phases/36-*/upstream/bug-report.md && grep -qE '[a-f0-9]{7,40}' .planning/phases/36-*/upstream/bug-report.md`
    - Commit landed: `git log --oneline -1 | grep -q 'docs(phase36-04)'`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md && test "$(wc -w < .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md)" -ge 500 && ! grep -qE '\{TBD\}|TODO' .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md && test -s .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/upstream-labels.txt && (grep -qx 'confirmed-bug' .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/resolved-bug-label.txt || test -f .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/upstream-labels-changed.md) && grep -qiE 'confirmed-bug|approved-feature|bug-confirmed|feature-approved' .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md</automated>
  </verify>
  <done>Live CONTRIBUTING.md fetched + normalized labels captured; bug-report.md exists, ≥500 words, no TBD placeholders, all 8 sections present, quotes and references intact, operative bug-PR label resolved to `confirmed-bug` (or rename memo written), CONTRIBUTING URL + fetch timestamp cited; commit on `sessions/codex-adapter-fix`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (CHECKPOINT): User files the issue; executor records URL + writes session status</name>
  <read_first>
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/upstream/bug-report.md (the body to paste)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/resolved-bug-label.txt (operative bug-PR label)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/contributing-url.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/contributing-fetch-ts.txt
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-RESEARCH.md (§7)
    - .planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/evidence/fork-url.txt
  </read_first>
  <what-built>
    The bug-report draft from Task 1 is complete, with the live-fetched bug-PR label gate
    resolved. All evidence is on disk. The user now needs to actually submit the issue to
    gsd-build/get-shit-done. This is the deliberate manual gate in the phase.
  </what-built>
  <how-to-verify>
    1. Executor prints the path to the draft and the submission URL:
       `cat .planning/phases/36-*/upstream/bug-report.md`
       Submit at: https://github.com/gsd-build/get-shit-done/issues/new/choose
    2. User copies the bug-report.md body into the issue form, submits, and pastes the
       resulting issue URL back to the executor.
    3. If `gh` CLI is authenticated on this machine, the executor MAY offer:
       `gh issue create --repo gsd-build/get-shit-done --title "<title from bug-report.md>" --body-file .planning/phases/36-*/upstream/bug-report.md`
       Record the returned URL.
    4. If `gh` is not available and the user doesn't respond within a reasonable window,
       HALT — do NOT fabricate an issue number. Write a memo describing the stall.

    After the URL is known, write `.planning/sessions/codex-adapter-fix-status.md` using
    the template in the <interfaces> block, including:
    - `**Bug-PR label gate:**` line citing the resolved token from evidence/resolved-bug-label.txt,
      the URL from evidence/contributing-url.txt, and the fetch timestamp from
      evidence/contributing-fetch-ts.txt.
    - Root cause (2-3 sentences).
    - Fix (2-3 sentences).
    - Evidence (2-3 bullets citing patched/control spawn_count and 7-criterion pass/fail).
    - Gotchas (3-5 bullets: install command from evidence/fork-install-cmd.sh, generator
      path from 36-02-SUMMARY.md, three STOP conditions, and the fact that the fork's
      main is NOT the upstream's main).

    Enforce ≤ 500 words.

    **W6 — DEFERRED escape requires verbatim user confirmation** (CONTEXT locks "file bug
    report FIRST"). A silent `DEFERRED` marker is NOT sufficient. If the user elects to
    defer, the executor MUST obtain (and then record in session-status.md) a line of the
    form:
    ```
    User confirmed defer on YYYY-MM-DD: <verbatim user rationale>
    ```
    The date MUST be ISO-8601 (YYYY-MM-DD). Without that line, the `DEFERRED` marker alone
    does NOT satisfy acceptance — Task 2 will fail. This prevents silent bypass of the
    CONTEXT.md gate.

    Final commit: `docs(phase36-04): file upstream issue + session closeout — <issue-URL>`
    (or `docs(phase36-04): DEFER upstream filing per user confirmation — <date>` if
    deferred with confirmation).
  </how-to-verify>
  <files>.planning/sessions/codex-adapter-fix-status.md</files>
  <action>
    Present the bug-report draft to the user (print path + submission URL). If `gh` CLI is
    authenticated, offer `gh issue create --repo gsd-build/get-shit-done --title "<title>" --body-file .planning/phases/36-*/upstream/bug-report.md`
    and capture the returned URL. Otherwise, wait for the user to paste the issue URL.

    Once URL known, write `.planning/sessions/codex-adapter-fix-status.md` (≤500 words) with:
    - `**Bug-PR label gate:**` line (resolved token + URL + timestamp from Task 1 evidence)
    - Root cause (2-3 sentences)
    - Fix (2-3 sentences)
    - Evidence (2-3 bullets citing patched/control spawn_count and 7-criterion pass/fail)
    - Gotchas (3-5 bullets: install command, generator path, three STOP conditions, fork's
      main vs upstream main distinction)

    **W6 — deferral requires explicit dated confirmation.** If user defers, write the file
    with "DEFERRED" at the top AND a `User confirmed defer on YYYY-MM-DD: <rationale>` line.
    Without the confirmation line, HALT — do NOT commit. The CONTEXT.md gate ("file bug
    report FIRST") is NOT bypassable via a silent DEFERRED marker.

    Do NOT fabricate an issue number. Commit:
    `docs(phase36-04): file upstream issue + session closeout — <URL>` OR
    `docs(phase36-04): DEFER upstream filing per user confirmation — <YYYY-MM-DD>`.
  </action>
  <verify>
    <automated>test -f .planning/sessions/codex-adapter-fix-status.md && test "$(wc -w < .planning/sessions/codex-adapter-fix-status.md)" -le 500 && (grep -qE 'https://github.com/gsd-build/get-shit-done/issues/[0-9]+' .planning/sessions/codex-adapter-fix-status.md || (grep -q 'DEFERRED' .planning/sessions/codex-adapter-fix-status.md && grep -qE 'User confirmed defer on [0-9]{4}-[0-9]{2}-[0-9]{2}' .planning/sessions/codex-adapter-fix-status.md))</automated>
  </verify>
  <done>session-status file exists, ≤500 words, records resolved bug-PR label gate with CONTRIBUTING URL + fetch timestamp, contains either a filed-issue URL OR a DEFERRED marker WITH a dated user-confirmation line (W6). Atomic commit on sessions/codex-adapter-fix.</done>
  <resume-signal>
    User pastes the GitHub issue URL (format: https://github.com/gsd-build/get-shit-done/issues/NNN),
    OR provides an explicit dated defer confirmation of the form
    "User confirmed defer on YYYY-MM-DD: <rationale>" to be pasted verbatim into session-status.md.
  </resume-signal>
  <acceptance_criteria>
    - `test -f .planning/sessions/codex-adapter-fix-status.md`
    - Word count ≤ 500: `test "$(wc -w < .planning/sessions/codex-adapter-fix-status.md)" -le 500`
    - Bug-PR label gate recorded with CONTRIBUTING URL + fetch timestamp:
      `grep -qi 'bug-pr label gate' .planning/sessions/codex-adapter-fix-status.md &&
       grep -qE 'raw\\.githubusercontent\\.com/gsd-build/get-shit-done' .planning/sessions/codex-adapter-fix-status.md &&
       grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' .planning/sessions/codex-adapter-fix-status.md &&
       grep -qiE 'confirmed-bug|approved-feature|bug-confirmed|feature-approved' .planning/sessions/codex-adapter-fix-status.md` (I1 — union grep tolerant of rename)
    - **W6 — issue URL OR dated user-confirmed defer**:
      `grep -qE 'https://github.com/gsd-build/get-shit-done/issues/[0-9]+' .planning/sessions/codex-adapter-fix-status.md \
        || (grep -q 'DEFERRED' .planning/sessions/codex-adapter-fix-status.md \
            && grep -qE 'User confirmed defer on [0-9]{4}-[0-9]{2}-[0-9]{2}' .planning/sessions/codex-adapter-fix-status.md)`
      A silent `DEFERRED` without the confirmation line FAILS acceptance.
    - Contains all 4 section headings (Root cause, Fix, Evidence, Gotchas):
      `grep -qi 'root cause' .planning/sessions/codex-adapter-fix-status.md &&
       grep -qiE '^## fix|^fix$' .planning/sessions/codex-adapter-fix-status.md &&
       grep -qi 'evidence' .planning/sessions/codex-adapter-fix-status.md &&
       grep -qi 'gotcha' .planning/sessions/codex-adapter-fix-status.md`
    - Final commit: `git log --oneline -1 | grep -q 'docs(phase36-04)'`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `test -f .planning/phases/36-*/upstream/bug-report.md` and `wc -w < .planning/phases/36-*/upstream/bug-report.md` ≥ 500
- `test -s .planning/phases/36-*/evidence/contributing-live.md` and `test -s .planning/phases/36-*/evidence/upstream-labels.txt` (B1 — live fetch artifacts present)
- Resolved bug-PR label recorded: `test -f .planning/phases/36-*/evidence/resolved-bug-label.txt` AND (`grep -qx 'confirmed-bug' .planning/phases/36-*/evidence/resolved-bug-label.txt` OR `test -f .planning/phases/36-*/evidence/upstream-labels-changed.md`)
- Union grep accepts known label variants: `grep -qiE 'confirmed-bug|approved-feature|bug-confirmed|feature-approved' .planning/phases/36-*/upstream/bug-report.md` (I1)
- `test -f .planning/sessions/codex-adapter-fix-status.md` and `wc -w < .planning/sessions/codex-adapter-fix-status.md` ≤ 500
- Session-status records the label gate + CONTRIBUTING URL + fetch timestamp (see Task 2 acceptance).
- Issue URL recorded, or `DEFERRED` + `User confirmed defer on YYYY-MM-DD` line present (W6).
- `git log --oneline | grep -c 'phase36-04'` — ≥ 1 (draft commit) or 2 (draft + closeout)
- Current branch is `sessions/codex-adapter-fix`; NO pushes to `main`.
- No PR opened against gsd-build/get-shit-done: `gh pr list --repo gsd-build/get-shit-done --author @me --state all 2>/dev/null | grep -c spawn-agent` = 0 (respecting the CONTEXT gate).
</verification>

<success_criteria>
- Live CONTRIBUTING.md fetched at filing time; operative bug-PR label resolved (B1).
- `upstream/bug-report.md` drafted, ≥ 500 words, all 8 sections present, evidence values substituted, CONTRIBUTING URL + fetch timestamp cited.
- Union grep tolerant of upstream label rename used throughout (I1).
- User has filed the issue (or explicitly deferred with a dated user-confirmation line — W6); URL captured.
- Session status file written, ≤ 500 words, 4 sections, citing issue URL + resolved bug-PR label gate with CONTRIBUTING URL + fetch timestamp.
- Atomic commit(s) on `sessions/codex-adapter-fix` (one for draft, one for closeout), zero on `main`.
- No upstream PR opened (gated by the resolved bug-PR label, per CONTEXT).
</success_criteria>

<output>
After completion, create `.planning/phases/36-fix-gsd-codex-skill-adapter-spawn-agent-tool-mismatch-with-c/36-04-SUMMARY.md`
documenting: upstream issue URL, filing date, any maintainer early response (if applicable),
resolved bug-PR label with CONTRIBUTING URL + fetch timestamp, and next-step plan
(wait for the resolved bug-PR label, then open PR from the fork).
</output>
