kind = "cubic_phase_scalar"
regime = "single_mode"
backend = "scalar_phase_extension"
description = "Normalized cubic spectral phase controlled by one scalar coefficient."
maturity = "experimental"
execution = "executable"
source = "lab_extensions/variables/cubic_phase_scalar.jl"
build_function = "build_cubic_phase_scalar_control"
projection_function = "project_cubic_phase_scalar_control"
units = "rad"
bounds = "unbounded"
parameterizations = ["full_grid"]
compatible_objectives = ["temporal_peak_scalar"]
dimension = 1
artifact_hooks = ["phase_profile", "group_delay"]
artifact_semantics = "Standard phase and group-delay diagnostics show the generated phase control."
validation = "Runtime contract and odd cubic-basis symmetry are regression-tested; scientific use remains experimental."
