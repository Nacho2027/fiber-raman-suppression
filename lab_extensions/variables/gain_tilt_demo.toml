kind = "gain_tilt_demo"
regime = "single_mode"
backend = "lab_extension"
description = "Demo planning contract for optimizing a smooth spectral gain or attenuation tilt."
maturity = "research"
execution = "planning_only"
source = "lab_extensions/variables/gain_tilt_demo.jl"
build_function = "build_gain_tilt_demo_control"
projection_function = "project_gain_tilt_demo_control"
units = "dB slope over the simulated optical bandwidth"
bounds = "bounded smooth gain/attenuation tilt; projection must enforce hardware-safe transmission limits"
parameterizations = ["low_order_polynomial"]
compatible_objectives = ["pulse_compression_demo", "raman_band"]
optimizer_representation = "small vector of low-order spectral gain coefficients mapped to a smooth multiplicative mask"
artifact_hooks = ["gain_tilt_profile", "shaped_input_spectrum", "energy_throughput"]
artifact_semantics = "Requires gain-profile plots, shaped-spectrum diagnostics, throughput metrics, and hardware-safety checks before execution."
validation = "Requires units, projection tests, energy-throughput tests, objective compatibility checks, artifact metrics, and a promoted backend before execution."
