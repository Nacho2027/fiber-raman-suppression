kind = "mode_weights_planning"
regime = "multimode"
backend = "lab_extension"
description = "Planning-only contract for optimizing multimode launch or modal weighting controls."
maturity = "research"
execution = "planning_only"
source = "lab_extensions/variables/mode_weights_planning.jl"
build_function = "build_mode_weights_planning_control"
projection_function = "project_mode_weights_planning_control"
units = "normalized modal power fractions"
bounds = "nonnegative weights projected to unit total power"
parameterizations = ["mode_basis"]
compatible_objectives = ["mmf_sum", "mmf_fundamental", "mmf_worst_mode"]
optimizer_representation = "mode-count vector projected onto the probability simplex"
artifact_hooks = ["mode_weight_bar_chart", "modal_power_table", "per_mode_leakage_table"]
artifact_semantics = "Requires modal weight tables, per-mode diagnostics, and output coupling interpretation before execution."
validation = "Requires units, simplex projection tests, gradient compatibility, artifact metrics, and a promoted backend before execution."
