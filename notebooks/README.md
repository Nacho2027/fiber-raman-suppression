# Notebooks

Notebooks are for inspection and scratch analysis. They are not the maintained
workflow surface.

Promote reusable code to `src/` or `scripts/lib/`. Promote runnable workflows to
`scripts/canonical/` only after tests and docs exist.
