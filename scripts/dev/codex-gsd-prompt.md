# Archived Agent Prompt

This prompt is kept for provenance only. Do not use it as current project
instructions. Current agent rules live in `AGENTS.md`, `AGENTS.md`, and
`agent-docs/current-agent-context/`.
