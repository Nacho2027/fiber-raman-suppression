# Phase Research Scripts

This directory contains phase-numbered research workflows. They are grouped by
phase to keep the public `scripts/` root focused on maintained entry points and
shared utilities.

Phase directories preserve historical research functionality. They should not
be treated as the public CLI surface unless promoted through
`scripts/canonical/`.
