# Tests

Run the fast tier:

```bash
make test
```

Python CLI tests:

```bash
make test-python
```

Slow and full tiers are for broader numerical checks and should run on suitable
compute:

```bash
make test-slow
make test-full
```
